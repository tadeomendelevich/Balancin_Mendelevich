# Balancin STM32 — Memoria del Proyecto
> Este archivo es la memoria persistente del firmware para Claude Code.
> Mantenerlo actualizado al final de cada sesión de trabajo.

---

## 🤖 Instrucciones Permanentes para Claude

**Estas reglas aplican en cada sesión, automáticamente y sin que te lo pida:**

### Al modificar cualquier archivo de código:
- Agregá una fila en "Registro de Cambios" con fecha actual (YYYY-MM-DD),
  archivo(s) tocado(s), qué cambió y por qué
- Si el cambio afecta parámetros PID, protocolo UART/UDP u otra sección
  del CLAUDE.md → actualizá esa sección también
- Si resolviste un bug → pasalo de "Pendientes" a "Funcionalidades completas"
- Si apareció un bug nuevo → agregalo en "Pendientes / bugs conocidos"
- Si tomaste una decisión de diseño importante → agregala en "Decisiones de Diseño"

### Al iniciar sesión:
- Leé este CLAUDE.md completo antes de hacer cualquier cosa
- Usalo como contexto del proyecto, no preguntes lo que ya está documentado acá

### Si un cambio afecta al otro proyecto (Qt ↔ STM32):
- Avisame explícitamente qué hay que cambiar en el otro proyecto
- Indicá el archivo exacto que necesita modificación

**Nunca rompas la estructura de este CLAUDE.md.**

---

## Descripción General
Firmware en **STM32CubeIDE** para el péndulo invertido "Balancín Mendelevich".
Implementa el control PID de estabilización, la lectura del IMU, el manejo de motores
y la comunicación bidireccional con la interfaz Qt (USB CDC + WiFi UDP).

---

## Rutas del Proyecto
| Proyecto | Ruta |
|----------|------|
| STM32 (este) | `C:\Users\tadeo\STM32CubeIDE\workspace_1.18.1\Balancin_Mendelevich` |
| Qt (interfaz) | `C:\Microcontroladores\BalancinQT` |

---

## Hardware
| Componente | Modelo | Descripción |
|------------|--------|-------------|
| Microcontrolador | STM32F411CEU6 (UFQFPN48) | MCU principal, 96 MHz (HSE 25 MHz + PLL) |
| IMU / Giroscopio | MPU-6050 | 6 ejes accel+gyro, I2C Fast, lectura DMA de 14 bytes, DLPF ~44 Hz |
| Driver de motores | Sin denominación en código (PWM directo) | Control PWM via TIM3/TIM4, 2 canales cada uno (dirección + velocidad) |
| Módulo WiFi | ESP-01 (ESP8266) | AT commands via USART1 a 115200 baud, UDP socket |
| Display | SSD1306 OLED 128×64 | I2C1, driver no bloqueante con DMA |
| Encoders | Cuadratura (modelo desconocido) | 4x quadrature por muestreo periódico (polling) a 4 kHz vía TIM2: PA8/PB13 (derecho), PB14/PB15 (izquierdo). `ENC_CPR=28` conteos/rev |

---

## Arquitectura de Archivos
```
Balancin_Mendelevich/
├── CLAUDE.md                        ← memoria del proyecto (este archivo)
├── Balancin_Mendelevich.ioc         ← configuración CubeMX (pines, periféricos)
├── Core/
│   ├── Src/
│   │   ├── main.c                   ← loop principal, init, PID, state machine, complementary filter
│   │   ├── MPU6050.c                ← driver IMU MPU-6050 (DMA, fixed-point, calibración/bias)
│   │   ├── ESP01.c                  ← driver WiFi ESP-01 (AT commands, UDP, watchdog)
│   │   ├── UNER.c                   ← protocolo binario UNER (parser RX, encoder TX, 37+ comandos)
│   │   ├── i2c_manager.c            ← gestor I2C no bloqueante con cola (size 8) y DMA
│   │   ├── ssd1306.c                ← driver display OLED SSD1306 no bloqueante
│   │   ├── fonts.c                  ← fuentes bitmap (7x10, 5x7) y logo UNER
│   │   ├── stm32f4xx_hal_msp.c      ← init periféricos HAL (ADC, I2C, TIM, UART)
│   │   ├── stm32f4xx_it.c           ← handlers de interrupción
│   │   ├── system_stm32f4xx.c       ← init sistema y reloj
│   │   ├── sysmem.c                 ← gestión de memoria newlib (_sbrk)
│   │   └── syscalls.c               ← syscalls newlib
│   └── Inc/
│       ├── main.h                   ← defines globales, pin LED (PB10), MPU_INT (PB12)
│       ├── MPU6050.h
│       ├── ESP01.h
│       ├── UNER.h                   ← enum comandos, structs LogData_t / WifiLogData_t
│       ├── i2c_manager.h
│       ├── ssd1306.h
│       ├── fonts.h
│       ├── stm32f4xx_it.h
│       └── stm32f4xx_hal_conf.h
├── USB_DEVICE/
│   ├── App/
│   │   ├── usb_device.c/h
│   │   ├── usbd_cdc_if.c/h          ← interfaz CDC USB (RX → UNER parser, TX → telemetría CSV)
│   │   └── usbd_desc.c/h
│   └── Target/
│       └── usbd_conf.c/h
├── tests/
│   └── test_ESP01.c                 ← tests unitarios del driver ESP-01
├── Drivers/
│   └── ...                          ← HAL STM32F4 V1.28.3, CMSIS
└── Middlewares/
    └── ...                          ← ST USB Device Library (CDC)
```
> Actualizar si se agregan o renombran archivos.

---

## Comunicación con Qt
### Canal 1 — USB CDC
| Campo | Valor |
|-------|-------|
| Periférico STM32 | USB OTG FS (PA11=DM, PA12=DP), clase CDC |
| Baudrate | N/A (USB CDC, velocidad nativa USB FS) |
| Formato de trama RX | Protocolo UNER binario: `"UNER"` + nBytes + `':'` + cmd + payload + checksum |
| Formato de trama TX | Mismo protocolo UNER; telemetría CSV decimada (LOG_DECIM=5 → ~20 Hz a 100 Hz loop) |

### Canal 2 — WiFi UDP
| Campo | Valor |
|-------|-------|
| IP del PC destino (Qt) | **Variable según ubicación** — ver tabla de perfiles abajo |
| Puerto de escucha STM32 (RX) | `30000` (LocalPORT en `ESP01_StartUDP`) |
| Puerto de envío al PC (TX) | `30010` (RemotePORT en `ESP01_StartUDP`) |
| Frecuencia de telemetría | ~10 Hz (LOG_WIFI_DECIM=10 sobre loop de 100 Hz) |
| Formato paquete WiFi | `WifiLogData_t` binario packed: t_ms, roll, output, PID terms, mR, mL, dt, dyn_sp, line data, 4×ADC |

> ⚠️ **La IP del PC destino cambia según la red donde se trabaje.** Antes de flashear, verificar
> que `wifiIp` en `main.c` (línea ~244) coincida con la IP actual del PC con Qt.
> Perfiles disponibles en el código (descomentar el que corresponda):
>
> | Red | SSID | IP del PC |
> |-----|------|-----------|
> | Casa (activo) | `MEGACABLE FIBRA-2.4G-ckd0` | `192.168.100.5` |
> | FCAL / Universidad | `FCAL` | `172.23.205.98` |
> | Delco Mendelevich | `Delco_Mendelevich` | `192.168.1.55` |
> | Wifi Habitaciones | `Wifi Habitaciones` | `192.168.1.48` |
>
> Si agregás una red nueva → añadí una fila acá y un perfil comentado en `main.c`.

---

## Control PID
| Parámetro | Variable en código | Valor actual |
|-----------|-------------------|--------------|
| Kp | `KP` / `KP_value` | `4.0` |
| Ki | `KI` / `KI_value` | `0.1` |
| Kd | `KD` / `KD_value` | `0.12` |
| Setpoint (ángulo °) | `SETPOINT_ANGLE` | `0.0°` (+ `setpoint_trim` ajustable en runtime) |
| Frecuencia de control | TIM1 (Prescaler=9599, Period=99) | **100 Hz** (10 ms/ciclo) |
| Filtro de derivada | Sin filtro explícito en derivada; zona suave (soft-zone) | SOFT_ZONE_ANGLE=1.5°, scale_min=0.15 |
| Integral anti-windup | `I_MAX` | ±100.0 PWM units; decay=0.990 por ciclo |
| Freno por velocidad | `KV_BRAKE` / `KV_BRAKE_STRONG` | 0.0 / 5.0 (umbral BRAKE_VEL_THRESHOLD=1.5) — velocidad calculada por encoders |

**Sensor de ángulo:**
- Fuente: Filtro complementario (α=0.98) entre acelerómetro y giroscopio del MPU-6050
- Eje de control: **roll** (inclinación lateral del péndulo)
- Bias del MPU hardcodeado (`MPU_USE_FIXED_BIAS`): ax=-46, ay=4950, az=1980, gx=-441, gy=-107, gz=-54

**Detección de caída (histéresis):**
- Caída: |roll| > 60°, recuperación: |roll| < 2°
- Boca abajo: |roll| > 120°; zona muerta (motores off): 35°–120°

---

## Periféricos STM32 Usados
| Periférico | Función | Pin(es) | Configuración |
|------------|---------|---------|---------------|
| TIM1 | Interrupción control loop 100 Hz | — (interno) | Prescaler=9599, Period=99 |
| TIM2 | Trigger ADC (TRGO) + muestreo de encoders cada 250 µs (4 kHz) | — (interno) | Prescaler=95, Period=249, TRGO. IT habilitada (`HAL_TIM_Base_Start_IT`), prio NVIC=3 |
| TIM3 | PWM motor (2 canales) | PB4=CH1, PB5=CH2 | Prescaler=0, Period=959 → ~100 kHz |
| TIM4 | PWM motor (2 canales) | PB6=CH1, PB7=CH2 | Prescaler=0, Period=959 → ~100 kHz |
| TIM5 | Sin uso desde 2026-07-01 (era el re-habilitador de EXTI de encoders, ya no aplica) | — (interno) | Inicializado (`MX_TIM5_Init`) pero nunca arrancado — no genera IT |
| I2C1 | IMU MPU-6050 + Display SSD1306 | PB8=SCL, PB9=SDA | Fast mode (400 kHz), DMA RX/TX |
| USART1 | Módulo WiFi ESP-01 (AT commands) | PA9=TX, PA10=RX | 115200 baud, async, IT RX byte a byte |
| USB OTG FS | Comunicación CDC con Qt | PA11=DM, PA12=DP | Device Only, CDC FS |
| ADC1 | 8 canales sensores (línea + analógicos) | PA1–PA7, PB0 | DMA circular, trigger TIM2, 15 ciclos/canal |
| GPIO PB10 | LED_BLINKER | PB10 | Output |
| GPIO PB12 | MPU_INT (EXTI12) | PB12 | Input, interrupción data-ready, EXTI15_10_IRQn prio 2 |
| GPIO PA8 | Encoder derecho canal A | PA8 | Input pull-up simple (sin EXTI desde 2026-07-01), leído por polling en TIM2 |
| GPIO PB13 | Encoder derecho canal B | PB13 | Input pull-up simple (sin EXTI desde 2026-07-01), leído por polling en TIM2 |
| GPIO PB14 | Encoder izquierdo canal A | PB14 | Input pull-up simple (sin EXTI desde 2026-07-01), leído por polling en TIM2 |
| GPIO PB15 | Encoder izquierdo canal B | PB15 | Input pull-up simple (sin EXTI desde 2026-07-01), leído por polling en TIM2 |
| GPIO PB2 | CH_PD ESP-01 (enable módulo) | PB2 | Output |
| GPIO PA0 | KEY (botón usuario) | PA0 | Input pull-up |
| GPIO PC13 | LED integrado | PC13 | Output |

> **IMPORTANTE:** No modificar pines sin actualizar el `.ioc` en CubeMX primero.

---

## Estado Actual
- **Etapa:** Casi terminado
- **Última sesión:** 2026-05-08

### Funcionalidades completas ✅
- PID de estabilización (balance) con zona suave y anti-windup
- Lectura IMU MPU-6050 vía DMA con bias hardcodeado (arranque instantáneo)
- Filtro complementario (α=0.98) acelerómetro + giroscopio
- Display OLED SSD1306 no bloqueante (actualización asíncrona via DMA)
- Máquina de estados del robot (IDLE, BALANCE_ONLY, BALANCE_AND_SPEED, LINE_FOLLOWING, MANUAL_CONTROL, MOTOR_TEST)
- Comunicación USB CDC con protocolo UNER binario (37+ comandos)
- Comunicación WiFi UDP via ESP-01 con watchdog y reconexión automática
- Telemetría en tiempo real: CSV por USB (~20 Hz) y binario por WiFi (~10 Hz)
- Tuneo en tiempo real de Kp, Ki, Kd, setpoint, steering desde Qt
- Seguidor de línea con 8 sensores ADC, PID de línea (Kp=10, Kd=2, Ki=0.5), velocidad en lazo cerrado por encoders y steering directo proporcional al error
- Detección de objetos en modo línea: ADC 5-8 (largo alcance) con debounce 30 ms → pasa a IDLE automáticamente al detectar obstáculo
- Control manual remoto (FORWARD/BACKWARD/LEFT/RIGHT/STOP)
- Freno dinámico por velocidad de encoders (KV_BRAKE_STRONG=5.0, umbral 1.5 m/s)
- Detección de caída y recuperación con histéresis
- Gestor I2C no bloqueante con cola (evita bloquear el loop de control)
- Encoders de cuadratura 4x: PA8/PB13 (derecho), PB14/PB15 (izquierdo), decodificados por muestreo periódico a 4 kHz vía TIM2 (ver Registro de Cambios 2026-07-01)
- Velocidad real de ruedas desde encoders (reemplaza estimación accel+gyro)
- **Odometría de pose (x, y, θ)** integrada a 100 Hz: encoders (distancia) + gyro Z (rumbo). Comandos UNER `GET_ODOMETRY=0xDA` / `RESET_ODOMETRY=0xDB`; origen se resetea al cambiar de modo. Pendiente verificar `ODOM_THETA_SIGN` en el robot (2026-07-04)
- **Retorno por odometría al punto de pérdida de línea** (`LOST_FWD`, camino centrado post-180°): navega a la pose guardada del último punto con línea visible en vez de avanzar a ciegas; display "VUELVE" (2026-07-04, pendiente validar signo de steering en el robot)

### Cambios recientes (2026-05-27)
- **6 bugs de stale-state corregidos** en la máquina de evasión: `obj_rev_initialized`, `obj_brake_start_ms`, `obj_rot_initialized`/`obj_rot_start_ms`, `line_error_f_d`, `line_obj_rev_vel_integral` — todos causaban saltos prematuros de estado al recuperarse de una caída
- `KV_brake_value` ahora efectivamente usado en `ComputeBrakeSetpointTarget` (antes el slider de Qt no tenía efecto)
- `obj_hold_initialized` (dead code) eliminado
- **Wall-following implementado**: OBJ_ARC reemplazado por `OBJ_WALL_FWD` + `OBJ_WALL_TURN`. Usa ADC7 (`adcAvg[6]`) como sensor lateral. Cadena activa: FOLLOWING → WAIT(1s) → REVERSE(10c) → BRAKE → ROTATE(325c) → HOLD(2s) → WALL_FWD ↔ WALL_TURN → FOLLOWING.
- `OBJ_WALL_FWD`: ángulo fijo `5.5°`, avanza mientras ADC7 < 3750. Si ADC7 < 300 ("demasiado cerca") → pivot derecha. Si ADC7 > 3750 → WALL_TURN.
- `OBJ_WALL_TURN`: pivot izquierda (potencia 20) hasta re-ver ADC7 < 3750. Si ADC7 < 300 → pivot derecha en cambio.
- `OBJ_DETECT_THRESHOLD_VAL` subido de `2000` → `3200` (sensores hardware actualizados con nuevas resistencias, mayor alcance)
- `OBJ_WALL_PIVOT_POWER` = 8.0 (subido de 5 que era insuficiente, bajado de 20 que era demasiado agresivo)
- Línea ignorada durante `OBJ_WALL_LINE_IGNORE_MS=3000ms` al entrar en WALL_FWD (evita volver a FOLLOWING prematuramente al cruzar la línea original)
- `rev_target_counts` reducido de `300` → `10` counts; hold pre-reversa reducido de 2s → 1s

### Pendientes / bugs conocidos 🔧
- El SSID/IP WiFi está hardcodeado en `main.c` (líneas ~244); cambiar manualmente según red
- `USBRxData` tiene el `UNER_PushByte` comentado — los comandos USB desde Qt no se procesan por esa ruta
- Código de debug activo en `ESP01.c` (printfs de estados AT) que genera tráfico USB extra
- **Freeze residual al mover el robot** — Causa raíz identificada (2026-05-18): el DMA del I2C queda en estado "en progreso" indefinidamente sin completar. `mpu_data_ready_for_ctrl` nunca se setea, `ControlStep10ms` nunca corre, `i2c1_tx_busy` queda en 1, display congelado, motores al último PWM. El `while(1)` SIGUE girando (IWDG se patea) pero el sistema es funcionalmente inútil. Fix: watchdog de software en `while(1)` — si pasan >150ms sin dato del MPU se llama `I2C1_Recover()` (9 pulsos SCL + HAL_I2C_Init + I2C_Manager_Init) y se relanza la lectura SIN resetear el MCU. Detectable por "I2C RECOVER\r\n" en USB. **2026-07-01: se sospecha que este freeze estaba agravado (no necesariamente causado) por el viejo sistema de encoders vía EXTI, que compartía prioridad NVIC con el DMA de I2C y podía dejar la transacción I2C pendiente en cola durante ráfagas de pulsos (ver Registro de Cambios 2026-07-01). Con encoders migrados a polling por TIM2 y el DMA de I2C subido a prioridad 0, debería ser mucho menos frecuente — pendiente de confirmar en el robot físico si desaparece del todo.**
- Resolución de velocidad limitada: piso de cuantización de `velocity_est` (calculado en `ControlStep10ms` sobre delta de counts cada 10ms, no sobre el muestreo de 4kHz de `SampleEncoders250us` que solo alimenta el conteo acumulado) ≈ 0.32 m/s con 1 count en una sola rueda por ciclo, ≈ 0.63 m/s si tiquean ambas ruedas. `BRAKE_VEL_DEADBAND=0.35` (2026-07-01) calibrado para filtrar ese piso. No apto para integración de posición ni para reaccionar a velocidades reales por debajo de ese umbral
- **Freeze + reset durante giro de 180° (LOST_ROTATE), reportado repetidamente pese a varios intentos de fix.** Sesión 2026-07-01: se probó primero bajar `LROT_PIVOT`/`LROT_BRAKE` (hipótesis de glitch eléctrico) + corregir prioridad NVIC I2C vs EXTI15_10 — el usuario confirmó que el problema **persistió igual**, descartando la hipótesis de corriente de motor como causa suficiente. El usuario migró el conteo de encoders de EXTI a polling por TIM2 (ver más abajo) sospechando que el bug estaba en el subsistema de encoders. Revisión de código no encontró un bug concreto en el nuevo esquema de polling — arquitectura correcta y con más margen que el EXTI viejo. **Pendiente de validar en el robot físico si el freeze+reset desaparece con el nuevo esquema de encoders.** Si persiste, revisar next: reversión brusca de motores (EMI) con un osciloscopio en VDD durante el pivot, o un logic analyzer en las líneas de encoder para confirmar que no hay rebote/ruido eléctrico real en el sensor.

---

## 📋 Registro de Cambios
> **Instrucción para Claude:** Al finalizar cada sesión, agregar una fila con los cambios realizados.

| Fecha | Archivo(s) modificado(s) | Cambio realizado | Motivo / Observación |
|-------|--------------------------|------------------|----------------------|
| 2026-07-05 | Core/Src/main.c | **Fix "el giro da distintas potencias a lo largo del recorrido, se desestabiliza y llega mal posicionado al freno"**: el lazo de tasa de giro (agregado el mismo día) reaccionaba al gyro Z **crudo**, que tiene ruido de vibración de motores (mismo ruido que causaba el tambaleo en balance) — cada pico de ruido producía un salto de pivot ciclo a ciclo, en vez de una potencia estable. Fix con dos variables nuevas de file-scope compartidas entre `LOST_ROTATE`/banco MANUAL (mutuamente excluyentes): `rot_yaw_filt_dps` (EMA de la tasa de giro, β=0.35, actualizada una sola vez por ciclo y continua entre fase 0/1) y `rot_pivot_f` (el pivot de salida con **slew-rate**: no puede cambiar más de 3 PWM por ciclo de 10ms, sin importar qué tan grande sea el error del lazo). Reemplazado el uso de `fabsf(gz/100)` instantáneo por `rot_yaw_filt_dps` en las 3 fases (avance, terminación, freno) y en el chequeo de `yaw_settled`. Reset de ambas variables al iniciar cada giro (`obj_rot_initialized` 0→1) y en el bloque de caída. | Usuario confirmó que el giro y el freno mejoraron pero seguía viendo potencia inconsistente durante el recorrido — la causa era ruido de sensor sin filtrar realimentado directo al lazo de control |
| 2026-07-05 | Core/Src/main.c | **Fix "se cae al terminar el giro de 180°, lo desestabiliza el freno"**: con el lazo de tasa ya funcionando bien (feedback del usuario: "seteé bastante bien los parámetros del giro"), el robot llega a la fase de frenado final girando bastante lento — pero esa fase seguía aplicando un **torque fijo** (antes 4 PWM constante durante hasta 250ms) sin importar la velocidad real remanente, y frenar de más invierte el sentido de giro, tirando el balance de costado. Reemplazado por **freno proporcional a la velocidad de giro medida por gyro** (`BRAKE_KP × yaw_dps`, clamp `[0, BRAKE]`): a full giro frena hasta el tope, a giro casi detenido no frena nada — autolimitado, no puede sobrepasar ni invertir el giro. Agregada salida temprana de la fase de freno cuando el giro ya se asentó solo (`\|gz\|/100 < 20°/s`), en vez de esperar siempre los 250ms completos. Aplicado en banco MANUAL (`MROT_*`) y `LOST_ROTATE` (`LROT_*`), sincronizados. Nota: `MROT_RATE_DPS`/`PIVOT_MAX` quedaron en 260°/s y 50 tras el ajuste del usuario en el banco — replicado a `LROT_*`. | Usuario confirmó que el giro en sí ya está bien calibrado; el único problema remanente era el freno de cierre desestabilizando el balance |
| 2026-07-05 | Core/Src/main.c | **Giro de 180° con lazo cerrado de velocidad de giro** (banco MANUAL `MROT_*` y `LOST_ROTATE` `LROT_*`, idénticos): el pivot fijo daba giros inconsistentes — unos a plena potencia y otros casi sin girar — porque a lazo abierto pelea contra la fricción estática y contra el `pwm_sat` del balance (que sumado/restado al pivot puede dejar una rueda con comando ~0 → rueda muerta por stiction → giro flojo). Ahora `pivot = feedforward(16) + KP(0.10) × (tasa_objetivo − tasa_medida_por_gyro)`, clamp [6, 30]: si gira lento la potencia sube sola, si se embala baja sola — consistente ante batería/fricción/estado del balance. Tasa objetivo `RATE_DPS=160°/s`, reducida por el slowdown (piso 25%) cerca del final; fase de terminación con el mismo lazo a target 40%. Freno final sincronizado a **4** PWM (ajuste manual del usuario en el IDE, replicado de MROT a LROT). Usa la escala del gyro corregida (gz/100). `EDGE_ROTATE`/`PERP_ROTATE` siguen a lazo abierto hasta validar. | Usuario reportó giros de potencia aleatoria (unos completos, otros casi nulos) — síntoma clásico de lazo abierto contra stiction variable |
| 2026-07-05 | Core/Src/main.c | **Fix "tambalea/vibra mucho en balance común"**: otra secuela del fix de escala del gyro — la corrección de yaw de BALANCE_ONLY (`-gz_dps*0.3`) y el yaw-assist del modo línea (`gz_dps*0.3`) quedaron efectivamente 31% más fuertes al corregirse la escala, y el ruido de gz (vibración de motores), amplificado por la compensación de deadband (±8 PWM sobre cualquier comando ≠0), producía shimmy constante. Ambas ganancias bajadas 0.3→**0.23** (=0.3×100/131, restaura la ganancia efectiva calibrada). | El valor 0.3 estaba calibrado sobre la escala vieja del gyro; al corregir la escala había que reescalar la ganancia |
| 2026-07-05 | Core/Src/main.c | **Giro de 180° "muy lento y siempre se cae al final" (banco MANUAL)**: dos causas en los parámetros — (1) la fase de potencia plena terminaba al 60% del recorrido (`ENC_FRAC=0.60`) y el 40% restante (72°) lo hacía la fase de terminación débil, más el slowdown recortando 55° antes del final → giro larguísimo; (2) el freno final fijo de 16 PWM×400ms era excesivo con el robot ya frenado por el slowdown — rebotaba el giro en sentido contrario y lo tumbaba (riesgo ya señalado en la revisión del 2026-07-01). Cambios en `MROT_*` (banco MANUAL) y `LROT_*` (LOST_ROTATE), mantenidos idénticos: `ENC_FRAC` 0.60→**0.85**, `SLOWDOWN_DEG` 55→**40**, `PIVOT` 14→**16**, `BRAKE` 16→**8**, `BRAKE_DURATION` 400→**250ms**. `EDGE_ROTATE`/`PERP_ROTATE` sin tocar hasta validar estos valores en el banco. | Feedback del banco de pruebas de MANUAL: giros lentísimos y caída sistemática al final |
| 2026-07-05 | Core/Src/main.c | **Modo MANUAL reconvertido en banco de pruebas del giro de 180°** (reemplaza el banco de pruebas de reversa, eliminado junto con `MANUAL_REV_TEST_TILT`): con el joystick en reposo, balancea quieto 2s (upright + freno de encoders) y ejecuta un giro de 180° con EXACTAMENTE la misma lógica y constantes que `LOST_ROTATE` (`MROT_*` = copia de `LROT_*`: ENC_TARGET 880, PIVOT 14, BRAKE 16, fases, guiado solo por encoder, gyro solo para overshoot, potencia de terminación 0.7/0.5), en ciclo indefinido — lo que se calibre acá vale directo para el giro real del seguidor. Joystick en uso → control manual normal (cancela el ciclo y rearma la espera). Nuevas variables `manual_test_rot_active`/`manual_test_wait_ms` (reseteadas en entrada a MANUAL y en caída); reusa las `obj_rot_*` (MANUAL y LINE son mutuamente excluyentes). | Usuario reportó que el giro de 180° sigue fallando y pidió un ciclo de giros constante cada 2s en MANUAL para poder calibrarlo viéndolo repetirse |
| 2026-07-05 | Core/Src/main.c | **CAUSA RAÍZ REAL del "ADC1 pegado viendo línea para siempre" encontrada y corregida**: `UpdateADC_MovingAverage` leía `adcValues[ch]` (buffer escrito por DMA a 4 kHz) DOS veces — una para el acumulador `adcSum` y otra para la ventana `adcBuf`. Si el DMA escribía entre ambas lecturas (probable justo en transiciones rápidas blanco↔negro), lo sumado ≠ lo guardado, y al restar esa posición de la ventana después quedaba un **residuo permanente** en `adcSum` — promedio inflado para siempre. Explica todo el historial: por qué reiniciar ADC/DMA nunca lo arreglaba (el hardware estaba sano, la corrupción era el acumulador de software), por qué la recuperación periódica con `clear_buffers=0` tampoco (no toca `adcSum` a propósito), y por qué casi siempre ADC1 (rank 1, convierte pegado al trigger). Fix: eliminado el acumulador incremental — una sola lectura por canal y el promedio se recalcula desde la ventana completa cada vez (32 sumas/10ms, costo nulo). `adcSum` eliminada. La recuperación periódica de ADC (3s) queda como defensa extra pero ya no debería ser necesaria para este síntoma. | Bug reportado repetidamente ("nunca se resolvió"); el fix anterior (2026-07-04, reinicio preventivo del DMA) atacaba el hardware cuando el problema era una carrera de lectura en el software del promedio móvil |
| 2026-07-05 | Core/Src/main.c | **Fix "el giro de 180° quedó lento y a pulsos (gira-frena-gira) hasta caerse"** — regresión de la corrección de escala del gyro (2026-07-04): los giros usaban `max(heading_gyro, heading_encoder)` y toda la calibración (targets, umbrales de fase, rampas) se hizo con el gyro sub-leyendo 24%; con el gyro corregido, el `max` adelantaba el pase a la fase 1 de terminación, cuya potencia débil (`PIVOT*0.4`, piso 0.2 → ~1-5 PWM) hacía la mayor parte del giro trabándose por fricción estática. Fix en los 3 giros (`LOST_ROTATE`/`EDGE_ROTATE`/`PERP_ROTATE`): (1) el heading de control es SOLO el del encoder (la fuente calibrada); el gyro queda únicamente para la detección de overshoot. (2) Potencia de la fase de terminación subida: `PIVOT*0.4`/piso 0.2 → `PIVOT*0.7`/piso 0.5 (~5-10 PWM) para que no se trabe arrancando ahora que el giro empieza desde parado de verdad (gate de quietud sostenida del mismo día). | Usuario preguntó si se había tocado la potencia del giro; no — la regresión venía del cambio de escala del gyro interactuando con una calibración empírica hecha con la escala vieja |
| 2026-07-05 | Core/Src/main.c | **Fix "sigue cayéndose en el giro de 180°" — el deadband en los gates de quietud (fix del 2026-07-04) era un error**: restar `BRAKE_VEL_DEADBAND=0.35` antes de comparar corría el umbral efectivo — `LOST_BRAKE` con "<0.15" salía del frenado con hasta **0.5 m/s reales** y el robot arrancaba el pivot rodando rápido → caída. Reemplazado por el enfoque correcto contra el ruido de cuantización: **velocidad CRUDA sostenida N ciclos** — `LOST_BRAKE`/`EDGE_WAIT` ahora exigen `\|velocity_est_f\| < 0.25` m/s durante 15 ciclos consecutivos (150ms, contador nuevo `line_quiet_cycles`, reseteado en entradas y caída); un pico de cuantización aislado solo resetea el contador un instante, y un robot rodando no puede mantenerla baja 150ms. Timeouts de colchón 1500→2500ms (frenar desde crucero tarda más). `LOST_SETTLE`/`EDGE_SETTLE`/`OBJ_BRAKE` vueltos a velocidad cruda con umbrales corregidos (0.25/0.25/0.15). Además, umbral de confirmación de pérdida en FOLLOWING 300→**150ms** — frenar aún antes para llegar al giro con la menor velocidad posible. | Usuario reportó que seguía cayéndose en el giro de 180° pese al fix del boost; el análisis reveló que el "fix de consistencia" del deadband había aflojado el gate de quietud en vez de endurecerlo |
| 2026-07-05 | Core/Src/main.c | **Fix "frena tan brusco al perder la línea que vuelca el robot"**: efecto colateral del disparo de pérdida adelantado a 300ms (mismo día) — antes el freno de `LOST_BRAKE`/`EDGE_WAIT` entraba con el robot ya desacelerado tras 1s de planeo, y el boost `LOST_BRAKE_BOOST=1.6` (agregado 2026-07-01 justamente porque ese freno tardío se sentía débil) era tolerable; con el freno entrando a velocidad de crucero plena y el robot aún inclinado hacia adelante, el tirón de 1.6× lo volcaba. `LOST_BRAKE_BOOST` 1.6→**1.0** (freno estándar de balance). Si queda corto, subir de a 0.1 — no volver a 1.6. | El boost compensaba un freno que llegaba tarde; al arreglar la tardanza, el boost pasó a ser excesivo |
| 2026-07-05 | Core/Src/main.c | **Fix definitivo del "giro fantasma de 90° al apoyar el robot viniendo del aire"** (`PERP_ROTATE`): el fix anterior (accel quieto 400ms) tenía dos agujeros — sosteniendo el robot firme en el aire el acelerómetro califica como "quieto", y en el aire `all_black` implica `line_detected` así que el timer de línea tampoco discrimina. Dos guardas nuevas en el disparo: (1) `all_black_elapsed < PERP_MAX_ELAPSED_MS=800ms` — un cruce real dura poco; viniendo del aire los 4 sensores llevan segundos en negro y queda bloqueado hasta que alguno vea blanco (lo que resetea el timer). (2) el todo-negro debe haber empezado a menos de `PERP_PARTIAL_WIN_MS=400ms` de la última "vista parcial" real (`last_partial_line_ms`, nuevo: línea detectada SIN los 4 sensores en negro — físicamente imposible en el aire). Un cruce perpendicular real cumple ambas: venía viendo línea parcial y el todo-negro es fresco. `last_partial_line_ms` se resetea al entrar al modo línea. | Usuario reportó que al apoyar el robot sobre la línea viniendo del aire SIEMPRE giraba 90° a la derecha, incluso la primera vez |
| 2026-07-05 | Core/Src/main.c | **Tres ajustes al retorno por odometría tras prueba en el robot**: (1) `LOST_FWD_TIMEOUT` 5000→**10000ms** — muy poco tiempo de búsqueda avanzando. (2) `LOST_RETURN_STEER_KP` 0.6→**1.5** y `LOST_RETURN_STEER_MAX` 12→**20** — "casi no corrige hacia el lado donde se perdió la línea"; además el formato compartido de motores aplica `half_steer=steering*0.5`, así que el efecto real por rueda era la mitad de lo nominal (documentado en el comentario de las constantes). (3) Umbral de confirmación de pérdida en FOLLOWING `ms_sin_linea > 1000` → **300ms** — frenar apenas se confirma la pérdida para no alejarse tanto del punto (~70cm menos a 1 m/s); 300ms sigue filtrando parpadeos de sensor. OJO: este umbral es compartido con el disparo del camino EDGE (pérdida por extremo), que ahora también reacciona antes. | Feedback del usuario tras probar el retorno: poco tiempo, poca corrección de rumbo, y frenaba demasiado tarde al perder la línea |
| 2026-07-04 | Core/Src/main.c | **Fix "VUELVE se queda totalmente detenido"**: usuario confirmó que el giro de 180° anda bien pero en el retorno por odometría el robot no arrancaba. Causa: `LOST_FWD_ANGLE_MAX=1.0°` y `LOST_FWD_SPEED_TARGET=0.18 m/s` (valores conservadores heredados del avance "a ciegas", bajados en su momento porque se aceleraba demasiado) — 1° de techo, atenuado además por la soft-zone del PID de balance con error chico, no vence la fricción estática (mismo patrón que el bug histórico de `OBJ_REVERSE` "no le da el ángulo"). Subidos: `LOST_FWD_SPEED_TARGET` 0.18→**0.40 m/s**, `LOST_FWD_ANGLE_MAX` 1.0→**2.5°**. Nota: estos valores también aplican a `EDGE_FWD` (comparten constantes) — vigilar que el avance post-giro de 45° no quede demasiado rápido; si molesta, separar constantes. | Con navegación a un punto conocido ya no hace falta el avance ultra-cauteloso; el techo de 1° directamente no movía el robot |
| 2026-07-04 | Core/Src/main.c | **Retorno por odometría al punto de pérdida de línea (camino "centrado")**: mientras sigue la línea, cada ciclo se guarda la pose actual en `line_loss_x_m`/`line_loss_y_m` (`line_loss_pose_valid`). Al perder la línea por el centro y completar el giro de 180°, `LOST_FWD` ya no avanza a ciegas: navega hacia esa pose guardada con steering P sobre el error de rumbo (`bearing = atan2(dy,dx)` vs `odom_theta_deg`, error normalizado a ±180°), `LOST_RETURN_STEER_KP=0.6` PWM/°, clamp `LOST_RETURN_STEER_MAX=12`. La velocidad la sigue regulando el PI existente (`LOST_FWD_*`). Salidas: línea → FOLLOWING (igual que antes); distancia al punto < `LOST_RETURN_REACHED_M=0.10m` sin línea → GIVEN_UP; timeout 5s queda como colchón. Snapshot invalidado al entrar al modo línea (el origen odométrico se resetea ahí). Display de LOST_FWD: "AVNZA"→"VUELVE". **Signo del steering derivado de `ODOM_THETA_SIGN`** (steering positivo genera gz negativo, convención del amortiguador `gz*0.3`): si en el robot curva alejándose del punto, verificar primero `ODOM_THETA_SIGN` con el test del giro de 90°, y si theta está bien, invertir el signo de `LOST_RETURN_STEER_KP`. El camino EDGE (pérdida por un extremo) no se tocó. | Usuario pidió usar la odometría nueva para volver al punto exacto donde se perdió la línea en vez del avance ciego con timeout |
| 2026-07-04 | Core/Src/main.c, Core/Inc/UNER.h, Core/Src/UNER.c | **ODOMETRÍA DE POSE (x, y, θ) — desarrollo nuevo**: integración a 100 Hz en `ControlStep10ms` — distancia por promedio de encoders (misma escala `ENC_CPR`/`ENC_VEL_SCALE` que `velocity_est`), rumbo por gyro Z integrado (menos sensible al slip que el diferencial de encoders). Variables `odom_x_m`/`odom_y_m`/`odom_theta_deg` (θ normalizado a (-180,180]); `ODOM_THETA_SIGN=+1.0` a verificar en el robot (si girar a la DERECHA hace θ más positivo, invertir a -1). No integra durante caídas (`f_fallen`). Origen (0,0,0°) se resetea al entrar a cualquier modo (BALANCE/LINE/MANUAL/IDLE) o con el comando nuevo `RESET_ODOMETRY`. **2 comandos UNER nuevos**: `GET_ODOMETRY=0xDA` (responde 3 floats LE: x[m], y[m], θ[°]) y `RESET_ODOMETRY=0xDB` (ACK). **Impacto en Qt**: para usarla hay que agregar los handlers de 0xDA/0xDB en `serialmanager.cpp` (o el equivalente UDP) — mientras no se agreguen, no pasa nada (comandos aditivos, no rompen lo existente). | Usuario pidió agregar odometría de pose como base para navegación futura (volver al punto donde se perdió la línea, esquives por trayectoria) |
| 2026-07-04 | Core/Src/main.c, Core/Src/UNER.c, Core/Inc/UNER.h, Core/Src/ESP01.c, Core/Src/MPU6050.c | **Barrida de bugs de auditoría completa (9 fixes + limpieza)**: (1) **Escala del gyro Z corregida**: `MPU6050.c` entrega gyro en centi-°/s (`raw*100/131`), pero los giros (LOST/EDGE/PERP/OBJ_ROTATE) y el yaw-assist usaban `gz/131` → leían solo el 76% del ángulo real; corregido a `gz/100` en los 6 lugares. Efecto esperado: el heading por gyro ahora es fiel (las transiciones de fase de giro pueden dispararse un poco antes; la salida sigue gobernada por encoder) y el amortiguador de yaw queda ~31% más fuerte — vigilar en el robot. (2) `usb_service_tx`: `chunk[64]` pasado a `static` — era stack pasado a `CDC_Transmit_FS` asíncrono (corrupción latente de toda la telemetría USB). (3) `UNER_SendData`: el camino USB no manejaba el wrap del ring buffer (tramas que cruzaban el final del buffer de 256 llegaban corruptas a Qt); ahora copia con wrap igual que `UNER_SendSerial`. Además se eliminaron los resets de `indexW/indexR` en `UNER_SendAllSensors`/`SendWifiLogData`/`SendLogData` que pisaban respuestas pendientes. (4) Nuevo `HAL_UART_ErrorCallback`: un overrun (ORE) en USART1 abortaba la recepción IT y el RX del ESP-01 quedaba muerto hasta reset; ahora limpia el flag y rearma. (5) Parser UNER: trama con `nBytes<2` hacía underflow a 255 en PAYLOAD y se tragaba 255 bytes; ahora se valida en NBYTES. (6) Watchdog de reconexión del ESP01: `ESP01_Task` corre cada 100ms (subtick 0), no cada 10ms — el umbral 2000 eran 200 s reales; bajado a 200 (=20 s). (7) Deadband de cuantización de encoders (`apply_deadbandf`) aplicado a las 5 esperas de quietud que faltaban: `LOST_BRAKE`, `EDGE_WAIT`, `LOST_SETTLE`, `EDGE_SETTLE`, `OBJ_BRAKE` (mismo bug ya arreglado el mismo día en OBJ_PRE_REVERSE_HOLD/pared). (8) Guard de `dt_real`: `micros()` envuelve cada ~44.7 s y metía un valor basura en la telemetría una vez por wrap. (9) Limpieza: `sendAllSensors` y `line_lost_entered_ms` (variables muertas) eliminadas, declaración duplicada de `espUSBBuf`/`espUSBBufIw/Ir` eliminada, chequeos muertos de `f_fallen` en LOST_FWD/EDGE_FWD removidos, comentarios desactualizados corregidos (debounce 100ms no 30ms, ventana ADC real 40ms no 3.75ms, sample rate MPU 100Hz no 200Hz, comentario de `REV_STRAIGHT_KP` actualizado al valor real -1.0). | Auditoría completa del firmware pedida por el usuario; todos los hallazgos con fix directo aplicados en la misma sesión |
| 2026-07-04 | Core/Src/main.c | **Nuevo estado `LINE_STATE_OBJ_WALL_CLEAR`**: al perder la pared en `OBJ_WALL_FWD` (`ADC7 >= OBJ_WALL_THRESHOLD`), en vez de pasar directo a girar (`OBJ_WALL_TURN`), ahora avanza `OBJ_WALL_CLEAR_COUNTS=100` counts de encoder más (mismo PI de velocidad y misma reversa/pivot de seguridad que `OBJ_WALL_FWD` si en el medio se acerca demasiado) antes de girar — para no quedar pivoteando pegado a la esquina/pared. Display: "LIBRE". Nuevas variables `obj_wall_clear_initialized`/`obj_wall_clear_r0`/`obj_wall_clear_l0` (snapshot de encoders, reseteadas en el bloque de caída). Agregado a la exclusión del limitador global de velocidad y a la lista de corrección yaw por gyro. **Fix de bug encontrado de paso**: la corrección de rumbo por gyro (aplicada cuando `line_pivot_active==0`) se activaba para `LINE_STATE_OBJ_WALL_FWD` sin chequear si `steering_adjustment` ya tenía la corrección real de la reversa pareja calculada — la pisaba en silencio. Ahora esa lista solo aplica si además `steering_adjustment == 0.0f` (es decir, el estado dejó el steering neutro a propósito para que el gyro lo asista, no cuando hay una corrección real activa). | Usuario pidió avanzar un poco más al perder la pared antes de girar, para no chocar la esquina; en el camino se encontró que la corrección de reversa dentro de `OBJ_WALL_FWD` podía estar siendo anulada silenciosamente por el asistente de rumbo por gyro |
| 2026-07-04 | Core/Src/main.c | **Freno más robusto en modo pared**: `OBJ_WALL_VEL_KP_BRAKE` 14→**22** (reacciona más fuerte a sobrevelocidad), `OBJ_WALL_BRAKE_ANGLE` 2.0→**4.0°** (más autoridad de frenado). Además, se encontró que el limitador global de velocidad (pensado para excluir estados que ya regulan su propia velocidad) no excluía a `OBJ_WALL_FWD`/`OBJ_WALL_APPROACH` — competía contra su PI propio comparando contra `LINE_SPEED_TARGET` (el target del seguidor de línea normal, no el de pared), quedó desactualizado al agregarles su propio control. Agregados a la exclusión. | Usuario reportó que a veces se sigue acelerando demasiado en modo pared pese al PI; el freno del PI era débil y además competía sin sentido con un limitador pensado para otro target de velocidad |
| 2026-07-04 | Core/Src/main.c | **`LINE_STATE_OBJ_WALL_APPROACH` sin control de velocidad** (encontrado en auditoría pedida por el usuario sobre qué estados sin línea regulan velocidad): usaba ángulo fijo `OBJ_WALL_APPROACH_ANGLE=2.0°` sin PI ni freno, mismo patrón que ya causaba aceleración descontrolada en otros estados antes de arreglarlos. Reemplazado por el mismo PI que `OBJ_WALL_FWD` (reutiliza `obj_wall_vel_integral`/`OBJ_WALL_VEL_KP`/`KP_BRAKE`/`KI`/`I_MAX`), con target más cauteloso `OBJ_WALL_APPROACH_SPEED_TARGET=0.3 m/s` y techo `OBJ_WALL_APPROACH_ANGLE_MAX=1.5°` (nuevas constantes; se eliminó `OBJ_WALL_APPROACH_ANGLE`). | Usuario preguntó si todos los estados sin línea controlaban velocidad; auditoría reveló que `OBJ_WALL_APPROACH` (avance post-giro buscando la pared) era el único que quedaba sin arreglar de ese patrón |
| 2026-07-04 | Core/Src/main.c | Ajuste de umbrales de `OBJ_WALL_FWD`/`OBJ_WALL_TURN`: `OBJ_WALL_REVERSE_THOLD` 300→**600** (ADC7 por debajo → reversa pareja), `OBJ_WALL_TOO_CLOSE_THOLD` 1200→1600→**2100** (ADC7 entre 600 y 2100 → pivot derecha). | Usuario pidió umbrales más altos para reversa/pivot, ajustado en dos pasos |
| 2026-07-04 | Core/Src/main.c | **Fix: la espera de quietud antes de retroceder (obstáculo y pared) no esperaba nada en la práctica**. Tanto `OBJ_PRE_REVERSE_HOLD` (0.10 m/s) como la espera de `OBJ_WALL_FWD`/`OBJ_WALL_TURN` (`OBJ_WALL_REVERSE_SETTLE_VEL=0.15 m/s`) comparaban contra `velocity_est_f` **sin filtrar** — pero esa velocidad cruda por encoders tiene un piso de ruido de cuantización de ~0.32-0.63 m/s (ver `BRAKE_VEL_DEADBAND`) aun con el robot totalmente quieto. Con umbrales de 0.10-0.15, la condición "está quieto" prácticamente nunca se cumplía, y ambas esperas caían siempre al timeout de seguridad y avanzaban igual — daba la impresión de que la pausa "no hacía nada". Filtrado con el mismo `apply_deadbandf(velocity_est_f, BRAKE_VEL_DEADBAND)` que ya usa el resto del código para este problema, en los tres puntos donde se compara (`OBJ_PRE_REVERSE_HOLD` y las dos ramas de wall-following). | Usuario reportó que la reversa arrancaba igual aunque el robot siguiera moviéndose; el umbral de velocidad estaba por debajo del piso de ruido real del sensor, un problema ya documentado y resuelto en otros lugares del código pero no replicado acá |
| 2026-07-04 | Core/Src/main.c | **`LINE_STATE_OBJ_PRE_REVERSE_HOLD` (pausa antes de retroceder al ver objeto) — ahora exige estabilidad real, no solo tiempo fijo**: antes salía únicamente por `OBJ_PRE_REVERSE_HOLD_MS=1000ms` de reloj, sin chequear si el robot realmente estaba quieto. Mismo patrón que `LOST_SETTLE`: sale por tiempo mínimo (1000ms) **Y** velocidad/inclinación estabilizadas (`OBJ_PRE_REVERSE_VEL_THR=0.10 m/s`, `OBJ_PRE_REVERSE_TILT_THR=3.0°`), con `OBJ_PRE_REVERSE_TIMEOUT=2500ms` como colchón de seguridad (no un corte normal) por si nunca llega a asentarse del todo. | Usuario pidió corregir la pausa previa al retroceso; el tiempo fijo no garantizaba que el robot estuviera realmente quieto antes de arrancar la reversa |
| 2026-07-04 | Core/Src/main.c | **ADC1 queda pegado en un valor alto para siempre después de ver negro real** — usuario confirmó que el baseline de calibración está bien (mide 2000 sobre blanco, igual al calibrado), así que no es un problema de calibración: después de un cruce real por línea negra, ADC1 se queda pinneado arriba (barra del display al tope) y nunca vuelve a bajar por sí solo, ni con el watchdog existente (`ADC1_Recover`, dispara solo si TODA la transferencia DMA de 8 canales se frena — si los otros 7 canales siguen actualizando, nunca detecta que uno solo quedó trabado). Fix: `ADC1_Recover` ahora recibe un parámetro `clear_buffers` — se agregó una recuperación **preventiva cada 3000ms** (`Core/Src/main.c`, bloque `is10ms`) que reinicia el hardware DMA/ADC sin necesidad de detectar la falla primero. Con `clear_buffers=0` en el barrido periódico (a diferencia de la recuperación por falla real, que sigue limpiando `adcSum`/`adcBuf`/`adcAvg` a 0), no se genera un bache de "todo blanco" cada vez que corre — el promedio móvil sigue con sus últimos valores válidos mientras el hardware se reinicia en microsegundos. | El mecanismo de recuperación existente estaba diseñado para detectar un freeze total del DMA, no un canal individual trabado en un valor fijo — un reinicio de rutina del periférico, sin tocar los buffers de software, es la forma más simple y de bajo riesgo de cubrir ese caso sin tener que implementar detección de canal congelado |
| 2026-07-04 | Core/Src/main.c | Ajuste de velocidad y umbrales de `OBJ_WALL_FWD`/`OBJ_WALL_TURN`: `OBJ_WALL_SPEED_TARGET` 0.4→**1.0 m/s**, `OBJ_WALL_REVERSE_THOLD` 250→**300** (ADC7 por debajo → reversa pareja), `OBJ_WALL_TOO_CLOSE_THOLD` 550→**1200** (ADC7 entre 300 y 1200 → pivot derecha). `OBJ_WALL_FWD_ANGLE` (3.5°) sin tocar — con el target tan alto puede terminar saturando contra ese techo sin llegar a 1.0 m/s, a confirmar en el robot. | Usuario pidió más velocidad de crucero y umbrales de reversa/pivot más amplios |
| 2026-07-04 | Core/Src/main.c | **Fix de giro de 90° falso al apoyar el robot recién levantado**: `LINE_STATE_PERP_ROTATE` (cruce perpendicular) exigía solo 50ms de "todos los sensores en negro sin manipular" — pero el timer `all_black_start_ms` venía arrastrando segundos acumulados desde que el robot estaba en el aire (ahí también lee "negro" al no haber superficie debajo). Apenas el acelerómetro dejaba de moverse (al apoyarlo), si en ese instante seguía viendo negro (punto de partida, mesa oscura, etc.) disparaba el giro de 90° al toque. Agregada una exigencia de quietud continua del acelerómetro (`ACCEL_SETTLE_MS=400ms`, nueva variable `accel_settled_start_ms`) antes de habilitar el disparo de `PERP_ROTATE` — recién dispara si además de ver negro sin manipulación, pasaron 400ms desde que se dejó de mover el acelerómetro. | Usuario reportó que apoyar el robot después de levantarlo siempre giraba 90° aunque no todos los sensores vieran negro en ese momento — el timer heredado del tiempo en el aire disparaba antes de que las lecturas reflejaran la superficie real |
| 2026-07-04 | Core/Src/main.c | **`OBJ_WALL_FWD`/`OBJ_WALL_TURN` — espera de estabilidad antes de la reversa por pared**: si al detectar `ADC7 < OBJ_WALL_REVERSE_THOLD` el robot todavía tiene velocidad residual de avance (`\|velocity_est_f\| > OBJ_WALL_REVERSE_SETTLE_VEL=0.15`), ahora frena primero (upright + freno de encoders, `ComputeBrakeSetpointTarget`) en vez de arrancar la inclinación de reversa de golpe con inercia hacia adelante — mismo patrón que `OBJ_PRE_REVERSE_HOLD` en la reversa por obstáculo. Además, ajuste de valores tras prueba en el robot: `OBJ_WALL_FWD_ANGLE` 2.5→**3.5°** (2.5 seguía quedando corto para avanzar) y `OBJ_WALL_SPEED_TARGET` 0.18→**0.4 m/s**. | Usuario pidió más quietud antes de iniciar la reversa por pared para no entorpecerla con inercia, y reportó que el ángulo de avance seguía quedando corto |
| 2026-07-04 | Core/Src/main.c | Ajuste de velocidad de `OBJ_WALL_FWD` tras el PI nuevo: `OBJ_WALL_SPEED_TARGET` 0.18→**0.4 m/s**, `OBJ_WALL_FWD_ANGLE` 1.0→**2.5°** (1.0° era muy poco para avanzar). | Usuario pidió más velocidad/ángulo que los valores iniciales conservadores del PI |
| 2026-07-04 | Core/Src/main.c | **Fix de "gira en círculos al entrar en modo línea"**: no era el ADC — el timer `all_black_start_ms` (usado para detectar cruce perpendicular, `LINE_STATE_PERP_ROTATE`) nunca se reseteaba ni al terminar ese giro de 90° ni al entrar al modo LINE_FOLLOWING. Si después del giro los 4 sensores seguían en negro (no había línea real debajo), el timer ya vencido re-disparaba otro `PERP_ROTATE` en el ciclo inmediato siguiente, y así indefinidamente. Agregado `all_black_start_ms = 0` en ambos puntos (fin de `PERP_ROTATE` ~línea 3895, entrada a modo LINE_FOLLOWING ~línea 2669). | Usuario reportó giro en círculos al entrar en modo línea sin línea real debajo; atribuido inicialmente a un ADC "trabado" pero el ADC no tenía nada que ver |
| 2026-07-04 | Core/Src/main.c | **`OBJ_WALL_FWD` (bordeando pared) — velocidad mucho más controlada**: reemplazado el ángulo fijo (`OBJ_WALL_FWD_ANGLE=4.5°`) + freno bang-bang por sobrevelocidad (`OBJ_WALL_OVERSPEED_VEL=0.90`, eliminada) por el mismo patrón de PI de velocidad que ya se usa en `LOST_FWD`/`EDGE_FWD`: `OBJ_WALL_SPEED_TARGET=0.18` m/s, `OBJ_WALL_VEL_KP=4.0`/`KP_BRAKE=14.0`, `OBJ_WALL_VEL_KI=1.0`/`I_MAX=1.0` con anti-windup, techo de ángulo bajado `OBJ_WALL_FWD_ANGLE` 4.5→**1.0°**, freno `OBJ_WALL_BRAKE_ANGLE` 1.0→2.0°. Nueva variable `obj_wall_vel_integral` (reseteada al entrar a `OBJ_WALL_FWD` desde `OBJ_WALL_APPROACH` o `OBJ_WALL_TURN`, y en el bloque de caída). | Usuario reportó que se acelera "una locura" bordeando la pared; el ángulo fijo saturaba contra el techo desde el primer ciclo de error, igual que el bug ya arreglado antes en `LOST_FWD`/`EDGE_FWD` |
| 2026-07-04 | Core/Src/main.c | **Wall-following (`OBJ_WALL_FWD`/`OBJ_WALL_TURN`) — reversa pareja si la pared está muy cerca**: antes, `ADC7 < OBJ_WALL_TOO_CLOSE_THOLD (500)` siempre pivoteaba a la derecha. Ahora hay dos umbrales: `ADC7 < OBJ_WALL_REVERSE_THOLD (250)` → retrocede usando el mismo corrector P de rumbo ya calibrado en `LINE_STATE_OBJ_REVERSE`/banco de pruebas de MANUAL (`REV_STRAIGHT_KP/MAX/SLEW`, misma variable de estado `obj_rev_steer_f`) — reutiliza directamente los valores ya afinados, sin duplicar lógica. `ADC7` entre 250 y `OBJ_WALL_TOO_CLOSE_THOLD` (subido de 500 a **550**) → pivot derecha, igual que antes. El setpoint de inclinación para la reversa (`OBJ_WALL_REVERSE_ANGLE=3.0°`) se agregó en el bloque de setpoints para ambos estados. Durante la reversa, `line_pivot_active` queda en 0 para que el formato compartido de motores (`half_steer=steering_adjustment*0.5`) aplique la corrección, en vez de escribir motores directo como hacen los pivots. | Usuario pidió que, en vez de solo girar cuando el sensor lateral ve la pared demasiado cerca, el robot retroceda derecho usando el mismo algoritmo de reversa ya calibrado, y deje el pivot solo para el rango intermedio |
| 2026-07-04 | Core/Src/main.c | **Reversa — simplificación completa desde cero (v10)**: pese al fix de `lroundf` (truncado a cero), la rueda izquierda seguía sin moverse en el banco de pruebas de MANUAL. Usuario confirmó que en modo balance normal ambas ruedas responden bien, descartando causa de hardware, y pidió repensar la lógica de reversa de la forma más simple posible e ir agregando complejidad de a poco. Se eliminó por completo el esquema de fracción de `pwm_sat` (v8/v9: `LINE_OBJ_REV_RATE_KP/KI/I_MAX`, `LINE_OBJ_REV_FRAC_MAX`, `LINE_OBJ_REV_STEER_SLEW`, variable `obj_rev_rate_integral`) y se reemplazó por un **P simple, sin integral, en PWM absoluto** (mismas unidades que `steering_adjustment`/`pwm_sat`, no una fracción): `REV_STRAIGHT_KP` (ganancia, arranca en **0.0** — paso 0, sin corrección, ambas ruedas reciben literalmente el mismo `pwm_sat`), `REV_STRAIGHT_MAX=8.0` (tope absoluto chico, no puede acercarse a `pwm_sat`), `REV_STRAIGHT_SLEW=2.0` (rampa). Aplicado igual en `LINE_STATE_OBJ_REVERSE` (rama sin línea) y en el banco de pruebas de MANUAL. Con `KP=0` el primer paso es puramente de diagnóstico: confirmar en el robot que ambas ruedas giran igual sin ninguna corrección; recién después subir `REV_STRAIGHT_KP` de a poco (2, 4, 6...) hasta que enderece, sin pasarse de un valor que empiece a notarse forzado. La variable `obj_rev_steer_f` cambió de semántica otra vez: ya no es una fracción, es la corrección rampeada en PWM absoluto — por eso ahora SÍ se puede sincronizar directamente con `steering_adjustment` en la rama con línea detectada (antes, con la fracción, sincronizarlas rompía la escala; con ambas en PWM absoluto no hay problema). | Dos rediseños seguidos (fracción v8, luego el fix de truncado v9-bis) no resolvieron el síntoma reportado — momento de simplificar al mínimo posible y verificar el caso base (sin corrección) antes de reintroducir cualquier lógica, en vez de seguir agregando mecanismos sobre una base no verificada |
| 2026-07-04 | Core/Src/main.c | **Bug real encontrado: motor izquierdo en 0 pase lo que pase con `MOTOR_LEFT_DEADBAND`**. Usuario subió mucho el deadband y el motor izquierdo seguía sin moverse — no era un problema de zona muerta. Causa: los casts `(int16_t)mL` / `(int16_t)mR` (4 lugares: fórmula compartida de `LINE_FOLLOWING`, banco de pruebas de MANUAL, camino normal de balance) truncan hacia cero en C, no redondean. Con la corrección de reversa reduciendo mucho una rueda (más con `FRAC_MAX` alto), el valor flotante de esa rueda podía quedar en ~0.4-0.6 y truncar a 0 exacto. Con comando=0, el bloque de compensación de deadband (`if (mL_comp > 0) ... else if (mL_comp < 0) ...`) nunca se activa — ningún valor de `MOTOR_LEFT_DEADBAND` puede arreglar un 0 literal. Fix: los 4 casts cambiados de truncado a `lroundf` (`(int16_t)lroundf(mL)`). Esto también explica por qué subir `FRAC_MAX` (0.6→0.8) empeoraba el síntoma en vez de mejorarlo: más reducción de la rueda floja = más probabilidad de caer en la zona de truncado a cero. | El síntoma "no se mueve ni con deadband grande" no encajaba con ningún ajuste de ganancia/fracción — apuntaba a que el comando nunca llegaba a ser distinto de cero en el punto donde se aplica la compensación, no a que la compensación fuera insuficiente |
| 2026-07-04 | Core/Src/main.c | **Ajuste de `LINE_OBJ_REV_FRAC_MAX` (v9) — rueda izquierda quedaba parada con el rediseño v8**. El giro en círculos desapareció (la garantía estructural de v8 se cumplió: `mR`/`mL` nunca invirtieron signo), pero con `FRAC_MAX=0.6` la asimetría permitida es tan grande (`mR`/`mL` hasta 1.6/0.4 = 4x) que, a la magnitud típica de `pwm_sat` durante esta reversa suave (`MANUAL_REV_TEST_TILT=4°`, `pwm_sat` de pocos PWM en crucero), el lado reducido caía por debajo de la zona muerta **mecánica real** del motor (fricción/stiction — no modelada en software más allá del offset fijo de ±1 de `MOTOR_LEFT/RIGHT_DEADBAND`, insuficiente para esto) y ese motor dejaba de girar del todo, aunque el PWM pedido matemáticamente no fuera cero. La garantía de signo de v8 sigue intacta (no puede volver a girar en círculos con ninguna ganancia); lo que cambió es cuánta asimetría es *físicamente* utilizable antes de que una rueda deje de moverse. Bajado `LINE_OBJ_REV_FRAC_MAX` 0.6→**0.30** (ratio máximo ~1.86x en vez de 4x) y `LINE_OBJ_REV_RATE_KP`/`KI` reducidos en proporción (3.0→1.5 / 1.0→0.4, más `I_MAX` 1.0→0.8) para no saturar el nuevo tope al instante con cualquier ruido de sensor. Si con 0.30 la rueda floja sigue casi detenida, el problema es puramente físico (zona muerta real más alta de lo esperado) y hay que seguir bajando `FRAC_MAX`, o subir `MANUAL_REV_TEST_TILT` para tener más `pwm_sat` de base y así más margen absoluto para ambos lados; si con 0.30 ya no corrige lo suficiente, subir `KP`/`KI` (nunca `FRAC_MAX` más allá del punto donde deje de mover alguna rueda). | Confirma que el rediseño estructural (v8) resolvió el problema que se buscaba resolver (giro en círculos); lo que sigue ajustando ahora es una interacción con un límite físico real del hardware (zona muerta del motor), no un bug de lógica |
| 2026-07-04 | Core/Src/main.c | **Rediseño completo de la corrección de reversa (v8) — el robot giraba en círculos con v7**. Usuario pidió replantear la lógica en vez de seguir ajustando ganancia/signo sobre el mismo diseño. Diagnóstico de fondo (más allá del signo, ya corregido en v7): la corrección se expresaba como un valor **absoluto** de steering (grados/PWM) con un clamp (`LINE_OBJ_REV_STEER_MAX_NOLINE`) totalmente independiente de `pwm_sat` (el comando de avance real, típicamente ~10-25 durante una reversa suave). Con ganancia alta, la corrección podía superar ampliamente a `pwm_sat` e invertir el sentido de giro de una rueda — eso ya explicaba el zigzag de v4, y con el clamp/ganancia aún más altos de v6/v7 pasó a invertir por completo una rueda de forma sostenida, produciendo giro en círculos en vez de reversa. Ningún valor de ganancia podía ser "correcto" sobre ese diseño porque su autoridad no tenía relación con la intensidad del comando que debía corregir. **Rediseño**: la corrección ahora se expresa como una **fracción de `pwm_sat`** (no un valor absoluto), acotada a `±LINE_OBJ_REV_FRAC_MAX=0.6` (<1.0 por diseño). Con `half_steer = frac·pwm_sat`, `mR = pwm_sat·(1-frac)`, `mL = pwm_sat·(1+frac)`, y `frac` siempre estrictamente entre -1 y 1, `mR`/`mL` **nunca pueden cambiar de signo** respecto a `pwm_sat` — el robot estructuralmente no puede terminar girando en el lugar, sin importar qué tan alta sea la ganancia. Esto es una garantía de diseño, no un ajuste fino. Nuevas constantes: `LINE_OBJ_REV_RATE_KP=3.0` (antes 45, ya no hace falta un valor extremo porque el clamp de fracción absorbe cualquier exceso de ganancia de forma segura), `LINE_OBJ_REV_RATE_KI=1.0` (antes 12), `LINE_OBJ_REV_RATE_I_MAX=1.0` (antes 5), `LINE_OBJ_REV_FRAC_MAX=0.6` (reemplaza `LINE_OBJ_REV_STEER_MAX_NOLINE`, eliminada), `LINE_OBJ_REV_STEER_SLEW=0.10` fracción/ciclo (antes 6.0°/ciclo). `obj_rev_steer_f` cambió de semántica: ahora almacena la fracción rampeada (±0.6), no grados — se corrigió también el punto donde se sincronizaba con el `steering_adjustment` absoluto de la rama con línea detectada (eso ya no tiene sentido de escala; ahora se resetea a 0 en ese punto). Aplicado tanto en `LINE_STATE_OBJ_REVERSE` como en el banco de pruebas de MANUAL (misma fórmula en los dos lugares). Pendiente de confirmar en el robot que ya no gira en círculos y que la asimetría se corrige razonablemente con esta ganancia; al ser una garantía estructural, subir `LINE_OBJ_REV_RATE_KP`/`KI` más allá de estos valores ya no puede volver a causar el giro en círculos — como mucho satura contra `FRAC_MAX` más rápido. | El usuario señaló correctamente que ningún ajuste de ganancia sobre el diseño anterior iba a ser "coherente" — hacía falta cambiar la estructura del controlador (acotar por fracción del comando de avance, no por un valor absoluto) en vez de seguir moviendo un número |
| 2026-07-04 | Core/Src/main.c | **Reversa por obstáculo — hipótesis de signo de v6 DESCARTADA (v7)**: probado en el nuevo banco de pruebas de MANUAL (ver entrada anterior). Con el signo invertido de v6, la rueda **izquierda** pasó a girar de más (antes, sin invertir, giraba de más la derecha) — es decir, se sobrecorrigió hacia el lado contrario en vez de corregir. Esto confirma que el signo original (v5, sin invertir) ya apuntaba en la dirección correcta; el síntoma repetido de v3/v4/v5 ("la derecha sigue retrocediendo más, no corrige") no era un bug de signo sino ganancia insuficiente para compensar del todo la asimetría mecánica real entre ruedas. Revertido el signo (sacado el `-` en ambos lugares: el `case LINE_STATE_OBJ_REVERSE` y el banco de pruebas de MANUAL, que comparten la misma fórmula). La ganancia EXTREMA de v6 (`LINE_OBJ_REV_RATE_KP=45`, `LINE_OBJ_REV_RATE_KI=12`) se mantiene sin cambios — con el signo ya correcto, debería ayudar a terminar de cerrar la asimetría real en vez de sobrecorregirla al revés. Pendiente de confirmar en el banco de pruebas de MANUAL si con el signo correcto y esta ganancia ya endereza bien, o si hay que ajustarla (subir más si sigue faltando autoridad, bajar si ahora sobrecorrige hacia la derecha). | El banco de pruebas de MANUAL (agregado en la sesión anterior) permitió aislar el problema y confirmar/descartar la hipótesis de signo en segundos, en vez de tener que repetir toda la secuencia de esquive en el robot para cada prueba |
| 2026-07-04 | Core/Src/main.c | **Modo MANUAL reconvertido en banco de pruebas de la reversa por obstáculo**: usuario pidió limpiar el código (el ciclo de giros automáticos 90/-90/180° con su máquina de estados de fases era la parte "desagradable") y usar el modo MANUAL para probar/afinar la corrección de rumbo de `LINE_STATE_OBJ_REVERSE` sin tener que pasar por toda la secuencia de esquive. Eliminado por completo: el ciclo automático de giros (`manual_seq_next_ms`/`manual_auto_rot_step`), la máquina de estados de rotación de 2 fases (`manual_rot_active`/`manual_rot_phase`/`manual_rot_heading`/`manual_rot_enc_counts`/etc.), las constantes `MANUAL_ROT_*`, y el registro UNER de comandos de rotación (`UNER_RegisterRotationCmd`). Comportamiento nuevo: con el joystick en reposo (idle: sin comando hace >60ms y ambos comandos casi en cero), MANUAL retrocede indefinidamente aplicando el mismo PI de diferencial de velocidad de ruedas que `OBJ_REVERSE` (mismas constantes `LINE_OBJ_REV_RATE_*` y mismas variables de estado `obj_rev_steer_f`/`obj_rev_rate_integral`, para que lo que se ajuste acá sirva directo para esa funcionalidad real), con `MANUAL_REV_TEST_TILT=4.0°` de inclinación hacia atrás. Si se manda un comando de joystick, el control manual normal sigue funcionando igual que antes (permite reposicionar el robot entre pruebas sin salir del modo). **Impacto en Qt**: los botones de giro preciso (`ROTATE_90_RIGHT/LEFT`, `ROTATE_180_RIGHT/LEFT`, `ROTATE_CUSTOM`) ya no tienen ningún efecto — quedan como no-op seguro (UNER.c ya verificaba puntero NULL antes de escribir, no hace falta tocar el protocolo). Si esos botones existen en la interfaz Qt, avisar al usuario que dejaron de hacer algo, o quitarlos de la UI si ya no aplican. | El ciclo de giros automáticos en MANUAL había sido un banco de pruebas para calibrar el giro de 90°/180° (ya migrado a `LINE_STATE_OBJ_ROTATE`/`LOST_ROTATE`/`EDGE_ROTATE`) y ya no aportaba valor una vez calibrado; reutilizar el mismo modo para la prueba de reversa evita crear un modo nuevo y reduce la superficie de estado del firmware |
| 2026-07-04 | Core/Src/main.c | **Dos cambios en la misma sesión**: (1) **`OBJ_REVERSE` sin línea (v6) — hipótesis de signo**: el PI de velocidad (v5, mismo día) seguía sin corregir la asimetría (rueda derecha retrocede más que la izquierda), mismo síntoma que con el P sobre acumulado (v3/v4) pese a ser un controlador distinto. Dos controladores distintos fallando igual con el mismo signo de corrección apunta a un error de signo, no de estructura/ganancia. Invertido el signo de la salida del PI (`steer_target = -(KP*rate_diff + KI*integral)`); si corrige al revés con este cambio, hay que sacar el `-`. A pedido del usuario, ganancias exageradas a EXTREMO para converger rápido en el robot: `LINE_OBJ_REV_RATE_KP` 18→**45**, `LINE_OBJ_REV_RATE_KI` 4→**12**, `LINE_OBJ_REV_RATE_I_MAX` 3→**5**, `LINE_OBJ_REV_STEER_SLEW` 3→**6**. Si con el signo invertido y estas ganancias tan altas TODAVÍA no corrige bien, el problema no es de signo — revisar si `speed_right_rps_s`/`speed_left_rps_s` tienen una asimetría de escala real entre ruedas (no solo de signo), o un problema mecánico/de sensor en el encoder derecho. (2) **Velocidad de búsqueda sin línea (`LOST_FWD`/`EDGE_FWD`) mucho más controlada**: usuario reportó que se acelera demasiado buscando la línea. `LOST_FWD_SPEED_TARGET` 0.45→**0.18 m/s**, `LOST_FWD_ANGLE_MAX` 2.0→**1.0°** (techo de aceleración mucho más bajo), `LOST_FWD_KP` 6.0→**4.0**, `LOST_FWD_KP_BRAKE` 10.0→**14.0** (reacciona más rápido a sobrevelocidad). Agregado término I (`LOST_FWD_KI=1.0`, `LOST_FWD_VEL_I_MAX=1.0`, con anti-windup igual que el PI de velocidad del seguidor de línea normal) para regular la velocidad de crucero con precisión en vez de depender solo de un P que saturaba contra el techo de ángulo. Nueva variable `lost_fwd_vel_integral` (file-scope, compartida entre `LOST_FWD` y `EDGE_FWD` porque son mutuamente excluyentes, reseteada en todas las entradas a esos estados, en el bloque de caída y al entrar al modo línea). | (1) Dos controladores distintos (P sobre acumulado, PI sobre tasa) fallando con el mismo síntoma tras varias vueltas de ajuste de ganancia es la señal clásica de un bug de signo, no de tuning; el usuario pidió exagerar para confirmar la hipótesis rápido en el robot en vez de seguir iterando a ciegas. (2) El P puro sin I saturaba contra el techo de ángulo desde el primer ciclo de error, sintiéndose como aceleración descontrolada en vez de una aproximación suave a la velocidad de crucero |
| 2026-07-04 | Core/Src/main.c | **Reversa por obstáculo sin línea (v5) — fix del zigzag**: usuario reportó que tras subir la ganancia (v4, mismo día) la rueda derecha retrocedía más que la izquierda y la corrección hacía zigzag en vez de enderezar. Causa raíz: la corrección era un P puro sobre el diferencial de distancia ACUMULADA (`rev_dr - rev_dl`), un término que crece sin límite durante toda la reversa (nunca decae). Con ganancia alta (2.20) y clamp ±45 muy por encima del `pwm_sat` típico de una reversa suave (~15-25), la corrección podía superar a `pwm_sat` e invertir el sentido de giro de una rueda — en ese momento el acumulado empezaba a moverse al revés, la corrección cambiaba de signo el ciclo siguiente, y se repetía: zigzag por sobrecorrección (no por signo invertido, que ya estaba bien). Fix: reemplazado por PI sobre la diferencia de **velocidad** instantánea entre ruedas (`speed_right_rps_s - speed_left_rps_s`), mismo patrón ya validado en `OBJ_ARC` (`OBJ_ARC_KP=15`/`OBJ_ARC_KI=3`). A diferencia del acumulado, la diferencia de velocidad se autolimita y vuelve a 0 en cuanto ambas ruedas igualan su velocidad — no hay ningún término que "recuerde" para siempre la asimetría del arranque, así que no hay de dónde sacar la sobrecorrección que causaba el zigzag. Nuevas constantes `LINE_OBJ_REV_RATE_KP=18.0`, `LINE_OBJ_REV_RATE_KI=4.0`, `LINE_OBJ_REV_RATE_I_MAX=3.0` (reemplazan `LINE_OBJ_REV_ENC_KP`/`LINE_OBJ_REV_GZ_KP`, eliminadas). Se mantiene el clamp `LINE_OBJ_REV_STEER_MAX_NOLINE=45` como techo (rara vez debería alcanzarse con este esquema) y el rate-limiter `LINE_OBJ_REV_STEER_SLEW=3.0°/ciclo` de la v4. Nueva variable `obj_rev_rate_integral` (file-scope, reseteada en los mismos puntos que `obj_rev_steer_f`: entrada a `OBJ_REVERSE`, salida a `OBJ_BRAKE`, caída, y puesta a 0 cada ciclo que se usa la rama con línea detectada para evitar windup). Pendiente de confirmar en el robot. | La v4 (P sobre acumulado con ganancia y clamp más altos) resolvió la falta de autoridad de corrección pero introdujo inestabilidad porque un P sobre un término sin límite natural, con ganancia suficiente para superar el comando de avance, es intrínsecamente propenso a sobrecorregir; un controlador sobre la tasa de error (no su acumulado histórico) es la forma estándar de evitar ese modo de falla |
| 2026-07-04 | Core/Src/main.c | **Reversa por obstáculo sin línea (v4)**: usuario reportó que la corrección seguía sin ser suficiente y que a veces el robot se queda en equilibrio sin retroceder al detectar el objeto. Cambios: (1) `LINE_OBJ_REV_ENC_KP` 1.50→**2.20** (más grados de corrección por count de diferencia entre ruedas). (2) Nuevo rate-limiter `LINE_OBJ_REV_STEER_SLEW=3.0°/ciclo`: el steering objetivo (`enc_term+rate_term`, clamp ±45 sin cambios) ya no se aplica de un salto a `steering_adjustment`, sino que rampea gradualmente vía nueva variable `obj_rev_steer_f` (file-scope, reseteada en todas las entradas/salidas de `OBJ_REVERSE` y en el bloque de caída). Motivo del rate-limiter: con `LINE_OBJ_REV_TILT_MAX=8.5°` (casi al límite de balance hacia atrás) y un clamp de steering de ±45° ya EXTREMO, un salto instantáneo de 0→45° en la corrección puede ser justo el empujón que tumbe al robot mientras está inclinado — y el código ya documentado (2026-07-02) hace que cualquier caída durante `LINE_STATE_OBJ_*` aborte la evasión y vuelva derecho a `FOLLOWING`, lo que coincidiría exactamente con el síntoma "se queda en equilibrio sin ir hacia atrás" (la caída+recuperación es rápida y se percibe como que nunca llegó a retroceder). También sincronizado `obj_rev_steer_f` con `steering_adjustment` en la rama con línea detectada, para que no haya salto al cambiar de rama a mitad de reversa. Pendiente de confirmar en el robot: si el síntoma de "se queda en equilibrio" persiste con el rate-limiter, la hipótesis de la caída silenciosa queda descartada y habría que revisar otra causa (p.ej. `OBJ_PRE_REVERSE_HOLD` de 1s, que es un balance intencional antes de retroceder y podría estar siendo percibido como el bug). | Corrección insuficiente pese a valores ya EXTREMOS en vueltas anteriores; se sospecha que la causa de "a veces no retrocede" es una caída real (por el propio yank de la corrección agresiva) que dispara el abort-on-fall ya existente para estados OBJ_*, no un fallo en la lógica de arranque de la reversa |
| 2026-07-03 | Core/Src/main.c | **Valores extremos a pedido del usuario para acotar el techo real de `OBJ_REVERSE`** — ambos síntomas ("no le da el ángulo" y "no endereza, izquierda gira menos") seguían pese a las vueltas anteriores. Hallazgo clave: un clamp GLOBAL (`sp_limit=5.0°`, aplicado a `base_setpoint_target` para *cualquier* estado que no sea `BALANCE_AND_SPEED`, en `ControlStep10ms` justo después del cálculo por estado) recortaba en silencio `LINE_OBJ_REV_TILT_MAX` cada vez que se subía por encima de 5.0° — el aumento a 6.0° de la vuelta anterior nunca se aplicó realmente, quedaba tapado en 5.0°. Cambios: (1) excepción en `sp_limit` para `LINE_STATE_OBJ_REVERSE` → **9.0°** (antes 5.0 sin excepción). (2) `LINE_OBJ_REV_TILT_MAX` 6.0→**8.5°** (EXTREMO). (3) `LINE_OBJ_REV_VEL_KP` 18.0→**28.0** (con vel_error=0.30 da 8.4°, casi todo el tope desde el primer ciclo). (4) rampa `sp_step_max` de OBJ_REVERSE 0.4→**0.6°/ciclo**. (5) `LINE_OBJ_REV_ENC_KP` (corrección de rumbo por encoders) 0.60→**1.5** y `LINE_OBJ_REV_STEER_MAX_NOLINE` 28→**45** (EXTREMO). Todos marcados en el código como "EXTREMO, bajar tras probar" — el objetivo es encontrar el techo real por prueba y error en el robot y después ajustar hacia abajo, no dejar estos valores como definitivos. Si la corrección de rumbo sigue favoreciendo el mismo lado (izquierda gira menos) incluso con el clamp ampliado, el signo de `LINE_OBJ_REV_ENC_KP` podría estar invertido — cambio de una línea si hace falta. | Dos vueltas previas de ajuste (KP y clamp por separado) no alcanzaron porque un límite global no documentado en este contexto (`sp_limit`) tapaba el efecto real de los cambios en el ángulo; el usuario pidió ir a valores extremos para encontrar el límite físico real antes de afinar |
| 2026-07-03 | Core/Src/main.c | **Ajuste de autoridad de corrección en reversa sin línea (v3.1)**: usuario confirmó que la corrección por encoders (v3, mismo día) "no corrige muy bien" pero al menos avanza — la rueda derecha viaja un poco más que la izquierda hacia atrás sin enderezarse del todo. Causa probable: el clamp de `steering_adjustment` en esa rama estaba fijo en ±15 en la llamada a `clampf_local`, y con `LINE_OBJ_REV_ENC_KP=0.6` una diferencia de apenas ~25 counts (menos del 10% del recorrido total de ~280) ya lo saturaba — la corrección probablemente estaba pegada al tope sin autoridad real para terminar de enderezar. Nueva constante `LINE_OBJ_REV_STEER_MAX_NOLINE=28.0` (antes literal `15.0f` en el clamp) — más margen de corrección diferencial, ganancia sin tocar. Pendiente de confirmar en el robot; si sigue sin alcanzar, subir `LINE_OBJ_REV_ENC_KP` (0.60) además del clamp. | El síntoma "corrige pero no del todo" apunta a autoridad de corrección insuficiente (clamp), no necesariamente a ganancia floja — si la ganancia ya satura el clamp con diffs chicos, subir solo la ganancia no habría ayudado |
| 2026-07-03 | Core/Src/main.c | `rev_target_counts` en `LINE_STATE_OBJ_REVERSE` subido de 200 (~10cm) a 280 (~14cm). | Usuario pidió un poco más de recorrido de reversa antes de pasar a `OBJ_BRAKE` |
| 2026-07-03 | Core/Src/main.c | **2do intento de fix "no le da el ángulo para empezar a retroceder"**: el usuario confirmó en el robot que, pese al fix anterior (mismo día, `LINE_OBJ_REV_VEL_KP=10.0`), seguía sin arrancar — el problema no era la velocidad para llegar al tope sino el tope en sí. Subidos juntos: `LINE_OBJ_REV_TILT_MAX` 3.5°→**6.0°**, `LINE_OBJ_REV_VEL_KP` 10.0→**18.0** (con `vel_error=0.30` da 5.4°, cerca del nuevo tope desde el primer ciclo). Además, rampa de `sp_step_max` específica para `LINE_STATE_OBJ_REVERSE` (0.15→**0.4°/ciclo**, antes compartía la rampa general de modo línea) — a 0.15°/ciclo tardaba ~400ms en llegar al ángulo pedido, ahora ~150ms. Pendiente de confirmar en el robot; si 6.0° tampoco alcanza, el próximo paso sería revisar si hay un límite físico más profundo (deadband de motor, fricción mecánica real del tren de reversa) en vez de seguir subiendo el ángulo. | Segunda vuelta del mismo síntoma tras confirmación del usuario de que el primer ajuste (subir solo la ganancia P, sin tocar el techo) no fue suficiente |
| 2026-07-03 | Core/Src/main.c | **Fix de zigzag en reversa por obstáculo sin línea visible (3er intento)**: el amortiguador de tasa por gyro puro (v2, 2026-07-02: `steering_adjustment = gz*0.6`) seguía dando zigzag pese a usar el mismo patrón "probado" de LOST/SEARCHING/etc. Diagnóstico: (1) un controlador de solo-tasa no tiene referencia de rumbo, no corrige desvío acumulado, solo frena la velocidad angular instantánea. (2) el eje Z del gyro es más propenso a ruido de vibración/EMI con los motores activos (mismo tipo de sensibilidad ya documentada en el freeze de I2C por EMI), y un controlador que reacciona solo a la tasa instantánea amplifica ese ruido. Reemplazado por P sobre el diferencial de distancia recorrida por encoders (`rev_dr - rev_dl`, ya calculado cada ciclo para medir progreso de la reversa) — refleja la asimetría física real entre ruedas sin depender del gyro, mismo enfoque (odometría, no inercial) usado en la primera versión de este código (2026-05-26) antes de migrar a gyro. Se mantiene un componente pequeño de amortiguación por gyro (`LINE_OBJ_REV_GZ_KP=0.15`, antes el único término con peso 0.6) para suavizar oscilaciones rápidas sin ser el driver principal. Nuevas constantes `LINE_OBJ_REV_ENC_KP=0.60`, `LINE_OBJ_REV_GZ_KP=0.15` — valores de partida, sin validar en el robot, signo de `LINE_OBJ_REV_ENC_KP` a confirmar físicamente (si corrige al revés, invertir el signo). | Dos intentos previos con gyro (PI integrado v1, luego amortiguador de tasa puro v2) dieron zigzag; se vuelve a un sensor distinto (encoders) menos sensible a vibración de motores para medir el desvío real |
| 2026-07-03 | Core/Src/main.c | **Fix real de "no le da el ángulo para empezar a retroceder" en `OBJ_REVERSE`** (reaparición del bug ya "arreglado" el 2026-07-02 subiendo `LINE_OBJ_REV_TILT_MAX` 2.0→3.5°). Causa raíz encontrada: el PI de velocidad de reversa usaba `LINE_VEL_KP=3.0` (compartida con el PI de avance normal) y `reverse_speed_target` tope en `LINE_OBJ_REV_SPEED_MAX=0.30` m/s — con el robot parado (vel=0), el término P inicial daba como máximo `3.0×0.30=0.9°`, muy por debajo del tope de 3.5° recién calibrado; alcanzar 3.5° dependía de que el integral hiciera windup durante ~8s (`2.5/(0.30×0.01)` ciclos), demasiado lento para vencer la fricción estática — el robot se quedaba "queriendo" arrancar igual que antes del fix de TILT_MAX. Agregada ganancia dedicada `LINE_OBJ_REV_VEL_KP=10.0` (antes reusaba `LINE_VEL_KP`), que con `vel_error=0.30` da `3.0°` de entrada en el primer ciclo — el tope de `LINE_OBJ_REV_TILT_MAX` se alcanza casi de inmediato en vez de esperar el windup del integral. Pendiente de confirmar en el robot físico. | El fix anterior (2026-07-02, subir TILT_MAX) elevó el techo permitido pero no el ritmo al que el PI llega a ese techo — el término P seguía saturado muy por debajo del tope al arrancar desde parado |
| 2026-07-01 | Core/Src/main.c | Fix freeze+reset en giro 180° (LOST_ROTATE), diagnosticado con 3 agentes en paralelo tras persistir a pesar de fixes previos: (1) `LROT_PIVOT` revertido 12.0→5.0 y `LROT_BRAKE` 8.0→3.0 — el commit de 2026-06-29 ya había bajado estos valores para evitar el spike de corriente al invertir el motor izquierdo (glitch VDD → CSS dispara NMI → reset), pero subieron de nuevo a 12/8 buscando reducir overshoot, reintroduciendo el problema. (2) Prioridades NVIC corregidas: `DMA1_Stream0/1_IRQn` (I2C1 RX/TX) subidas de prio 1→0; `EXTI15_10_IRQn` (encoders PB13/14/15 + MPU PB12) bajada de 1→2; `EXTI9_5_IRQn` (encoder PA8) alineada a 2 (antes 5 por defecto de CubeMX). Antes I2C DMA y EXTI15_10 empataban en prio 1 → durante el giro (máxima tasa de pulsos de encoder) el fin de transacción I2C podía quedar pendiente en cola, dejando la lectura del MPU colgada → PWM congelado en el último valor (alto, en pleno pivot) → robot acelerado y trabado hasta que el IWDG resetea. Pendiente de validar en el robot físico. | Bug recurrente reportado por el usuario, ya documentado varias veces en este changelog sin solución definitiva |
| 2026-07-01 | Core/Src/main.c | Revertido `LROT_PIVOT`/`LROT_BRAKE` de vuelta a 12.0/8.0 (deshecho el cambio del punto anterior). El usuario probó el fix de prioridades NVIC + pivot bajo y el freeze+reset siguió pasando igual — descarta la hipótesis de glitch eléctrico por corriente de motor como causa (al menos como causa suficiente). Usuario reporta que el problema aparece "siempre que uso los encoders", no solo en el giro de 180°, apuntando a un bug en el manejo de encoders (ISR/EXTI/timing) en sí, no a la magnitud del pivot. Queda pendiente investigación a fondo del subsistema de encoders (ver Pendientes). Prioridades NVIC del punto anterior (I2C DMA prio 0, EXTI15_10/EXTI9_5 prio 2) se mantienen sin revertir. | El fix anterior no resolvió el bug; nueva pista del usuario redirige la investigación al subsistema de encoders en general |
| 2026-07-01 | Core/Src/main.c | **Encoders migrados de EXTI (interrupción por flanco) a polling por TIM2 (250µs / 4kHz), hecho por el usuario directamente en el IDE.** Cambios: (1) nueva función `SampleEncoders250us()` que lee PA8/PB13/PB14/PB15 y decodifica cuadratura con tabla de transición de estados estándar (`quad_delta[16]`), llamada desde `HAL_TIM_PeriodElapsedCallback` en cada tick de TIM2. (2) GPIO de los 4 pines de encoder cambiado de `GPIO_MODE_IT_RISING_FALLING` a `GPIO_MODE_INPUT` simple — ya no generan EXTI. (3) Eliminado el código de conteo por EXTI dentro de `HAL_GPIO_EXTI_Callback`. (4) `EXTI9_5_IRQn` deshabilitado (`HAL_NVIC_DisableIRQ`). (5) `TIM5` (el "anti-storm masking" que re-habilitaba EXTI cada 2ms) dejó de arrancarse — queda inicializado pero inerte. (6) `TIM2_IRQn` agregado a las prioridades NVIC de USER CODE BEGIN 2, prio 3. Revisado exhaustivamente por Claude: tabla de decodificación correcta (estándar, usada en libs como PJRC Encoder), mapeo de pines consistente con el hardware, margen de Nyquist amplio (4kHz sampling vs ~470 counts/seg máximo teórico con motores de 1000 RPM — antes el límite EXTI anti-storm era ~500Hz/canal y podía perder pulsos reales en giros rápidos). Sin bugs de correctness encontrados. Pendiente de limpieza no urgente: `EXTI9_5_IRQHandler` en stm32f4xx_it.c queda huérfano, guards `if(EXTI->IMR & GPIO_PIN_13/14/15)` en `EXTI15_10_IRQHandler` nunca se cumplen, `MX_TIM5_Init()` sigue llamado sin necesidad, `tick2ms_count` congelado en 0 sin usarse. Los pines de encoder no están gestionados por el `.ioc` de CubeMX (confirmado por grep), una regeneración no debería pisar este cambio. | El usuario reportó que el freeze+reset en el giro de 180° "siempre pasa cuando uso los encoders" y que el fix de pivot/prioridades del punto anterior no lo resolvió — sospecha de bug real en el manejo EXTI de encoders (storm de interrupciones, carrera con TIM5 sobre `EXTI->IMR`, prioridad compartida con I2C). Migrar a polling elimina de raíz esa clase de problema: tasa de interrupción fija y acotada (4kHz) en vez de variable según velocidad física de la rueda. Pendiente de validar en el robot físico si el freeze+reset desaparece del todo |
| 2026-07-01 | Core/Src/main.c | `LROT_ENC_TARGET` (giro 180° en LOST_ROTATE) subido 760→829 counts (+8.3%) y `LROT_P1_MAX` 1500→1800ms. Tras la migración de encoders y el fix de signo, el giro quedaba ~15° corto (165° en vez de 180°). 829 = 760×(180/165), reescalado empírico proporcional al faltante reportado por el usuario. Pendiente de confirmar en el robot si el nuevo valor da 180° exactos o necesita un segundo ajuste fino. | Calibración de counts/grado quedó desactualizada tras el cambio de método de conteo de encoders |
| 2026-07-01 | Core/Src/main.c | Segunda pasada de calibración del giro 180°: `LROT_ENC_TARGET` 829→880 (seguía quedando corto). Además, `LROT_P1_MAX` 1800→6000ms: pasa de ser un corte normal a ser **solo** un colchón de seguridad (motor trabado / sensor fallando) — la salida de la fase de freno ahora depende casi exclusivamente de `enc_done` (completar los counts del encoder), no del tiempo transcurrido. Pendiente de confirmar en el robot si 880 ya da 180° exactos. | Usuario pidió explícitamente que el giro no corte "accidentalmente por tiempo" y solo salga al completar los pulsos de encoder |
| 2026-07-01 | Core/Src/main.c | Fix de "a veces sigue quedándose en GIRO": los colchones de seguridad `LROT_P0_MAX`/`LROT_P1_MAX` (4000/6000ms) y `EROT_P0_MAX`/`EROT_P1_MAX` (2500/3500ms) eran demasiado generosos — si algo impedía completar los counts de encoder a tiempo (fricción, superficie, etc.) el giro podía tardar hasta ~10s en salir por timeout, sintiéndose "pegado". Acortados a `1500/800` (180°) y `1000/800` (45°) — peor caso total ahora ~2.3s en vez de ~10s. Siguen siendo colchones de seguridad, no cortes normales (la salida normal sigue siendo completar el target de encoder). | Los timeouts de seguridad heredados de iteraciones anteriores (pensados para otro esquema de frenado) quedaron demasiado largos para el nuevo freno fijo de 400ms |
| 2026-07-02 | Core/Src/main.c | `rev_target_counts` en `OBJ_REVERSE` vuelto de 1000 (~50cm, temporal para calibrar) a **200 (~10cm)**, el valor normal — ya validado el enderezamiento por gyro. | Usuario confirmó que el enderezamiento ya anda bien y pidió volver a la distancia de reversa normal |
| 2026-07-02 | Core/Src/main.c | Fix de zigzag en la reversa por obstáculo: la corrección por PI de rumbo integrado (`obj_rev_heading` + `obj_rev_straight_int`, agregada en el punto anterior) hacía zigzag — señal de signo/ganancia mal calibrados en una fórmula nueva sin validar en el robot. Reemplazada por el mismo patrón de amortiguación por tasa (gyro Z crudo, SIN integrar) que ya se usa y funciona en LOST/SEARCHING/OBJ_HOLD/OBJ_WALL_FWD/LOST_FWD/EDGE_FWD: `steering_adjustment = clamp(gz_dps * 0.6, ±15)` (el 0.6 compensa el `*0.5` que se aplica después sobre `steering_adjustment` para llegar al mismo `half_steer` efectivo que ese patrón ya probado, que usa `gz_dps*0.3` directo). Eliminadas `LINE_OBJ_REV_GYRO_KP/KI`, `obj_rev_heading` y `obj_rev_straight_int` (quedaron sin uso). | La fórmula de PI de rumbo integrado, nueva y sin testear, resultó tener signo o ganancia incorrectos; más seguro reusar la convención ya validada en el resto del código que reinventar una |
| 2026-07-02 | Core/Src/main.c | **Fix real de "no le da el ángulo para ir hacia atrás"**: mismo bug de `balance_hold_active` ya visto en `LOST_FWD`/`EDGE_FWD` (2026-07-01), ahora en `LINE_STATE_OBJ_REVERSE` — el robot suele estar bien parado y nivelado justo al empezar a retroceder, así que el hold se activaba de entrada y apagaba el PID de balance antes de poder generar el ángulo de reversa. Agregado a la lista de exclusión de `balance_hold_active`. El aumento de `LINE_OBJ_REV_TILT_MAX` (2.0→3.5°) del punto anterior no alcanzaba porque el PID estaba directamente silenciado, no era un problema de límite de ángulo. | El síntoma "quiere retroceder pero no logra, se queda quieto" es el mismo patrón de bug de hold ya visto, esta vez en un estado nuevo que no se había cubierto |
| 2026-07-02 | Core/Src/main.c | Reversa por obstáculo (`LINE_STATE_OBJ_REVERSE`) mejorada a pedido del usuario: (1) **Enderezamiento por gyro Z** en vez de diferencial de encoders cuando no ve línea — el diferencial de encoders no distinguía asimetría mecánica real de un giro real; ahora integra `gz` (igual que los estados de rotación) y corrige PI (`LINE_OBJ_REV_GYRO_KP=0.8`, `LINE_OBJ_REV_GYRO_KI=0.05`) para mantener el rumbo en 0. Nueva variable `obj_rev_heading`, reseteada al entrar a `OBJ_REVERSE` y en el bloque de caída. (2) `LINE_OBJ_REV_TILT_MAX` subido de 2.0° a **3.5°** — con 2.0° a veces no alcanzaba para vencer la fricción estática y arrancar desde parado, el robot se quedaba "queriendo" retroceder sin lograrlo. (3) `rev_target_counts` alargado temporalmente de 200 (~10cm) a **1000 (~50cm)** para poder calibrar bien la rectitud — achicar de nuevo una vez ajustado el gyro. | Usuario pidió reversa recta usando el giroscopio en vez de encoders, más ángulo porque a veces no lograba arrancar, y más distancia para poder calibrar visualmente |
| 2026-07-02 | Core/Src/main.c | **Fix de "sigue frenando a cero" en el PI de velocidad de `LINE_STATE_FOLLOWING`** pese a bajar `sp_step_max` a 0.15: `line_forward_vel = fmaxf(0.0f, -velocity_est_f)` era la única lectura de velocidad de todo el firmware que seguía SIN el filtro de deadband — el mismo bug de cuantización ya arreglado en `LOST_FWD`/`EDGE_FWD` (un solo pulso de encoder simultáneo en ambas ruedas se lee como ~0.63 m/s de golpe). Aplicado `apply_deadbandf(·, BRAKE_VEL_DEADBAND)`. Explica por qué bajar la rampa no alcanzaba: el disparador de frenado se repetía constantemente por ruido, no por una rampa demasiado agresiva. | Se había aplicado el fix de deadband a LOST_FWD/EDGE_FWD pero se pasó por alto el PI de velocidad del seguidor de línea normal, que usa la misma señal cruda |
| 2026-07-02 | Core/Src/main.c | Si el robot se cae mientras está esquivando un obstáculo (cualquier `LINE_STATE_OBJ_*`), ahora abandona la evasión y vuelve directo a `LINE_STATE_FOLLOWING` en vez de retomar la secuencia de esquive donde había quedado al recuperarse. Se detecta con `line_state >= LINE_STATE_OBJ_PRE_REVERSE_HOLD` (son los últimos del enum). Se ignora la detección de objeto por 5s al volver (mismo patrón usado en otras salidas de evasión), por si el objeto sigue ahí. | Usuario pidió explícitamente que una caída durante el esquive cancele la evasión en vez de continuarla |
| 2026-07-02 | Core/Src/main.c | **Fix de "acelera y de golpe frena hasta quedar totalmente quieto" en seguidor de línea normal**: el "limitador global de velocidad" (agregado para frenar en LOST_FWD/EDGE_FWD/etc.) no excluía `LINE_STATE_FOLLOWING`, a pesar de que ese estado ya regula su propia velocidad con el PI de línea (`LINE_VEL_KP`/`LINE_VEL_KP_BRAKE`/`LINE_VEL_KI`, que calcula un ángulo de frenado gradual). Cada vez que la velocidad cruzaba el 90% de `LINE_SPEED_TARGET`, este limitador TAPABA el ángulo suave del PI con `ComputeBrakeSetpointTarget` (pensado para frenar del todo en balance estático, no para regular velocidad de crucero), produciendo un frenazo brusco hasta velocidad cero en vez de una regulación fluida. Agregado `line_state != LINE_STATE_FOLLOWING` a la exclusión. | El limitador global decía en su propio comentario que solo debía aplicar a estados "que no tienen su propio control de frenado" pero `LINE_FOLLOWING` sí lo tiene — quedó afuera de la lista de exclusión por descuido |
| 2026-07-02 | Core/Src/main.c | `LOST_FWD_TIMEOUT`/`EDGE_FWD_TIMEOUT` subidos de 3s a **5s** (se probó 7s primero, el usuario lo bajó) — más tiempo avanzando buscando la línea antes de pasar a `GIVEN_UP` (reposo total). | Usuario pidió más margen para reencontrar la línea antes de darse por vencido |
| 2026-07-02 | Core/Src/main.c | Dos funcionalidades nuevas a pedido del usuario: (1) **Si el giro (180° o 45°) termina ya sobre la línea, va directo a `FOLLOWING` sin pasar por `LOST_FWD`/`EDGE_FWD`** — se chequea `line_detected` en el mismo instante en que `LOST_ROTATE`/`EDGE_ROTATE` completan el freno fijo; si es verdadero, transición directa a `FOLLOWING` (reseteando integrales), si no, sigue el camino normal (`LOST_FWD` o `EDGE_SETTLE`). (2) **Nuevo estado `LINE_STATE_PERP_ROTATE`**: si durante `LINE_STATE_FOLLOWING` los 4 ADC leen negro simultáneamente Y el acelerómetro NO está variando (o sea, no lo levantaron con la mano — reutiliza `accel_motion_f` ya existente), con un debounce corto de 50ms (no los 2s de "en el aire"), se interpreta como cruce perpendicular a una línea (ej. una "+"): la franja cubre los 4 sensores de punta a punta. Dispara un giro instantáneo de 90° fijo (dirección fija a la derecha, no hay info de lado porque los 4 sensores ven lo mismo) con la misma lógica gz+encoder y freno fijo corto que `LOST_ROTATE`/`EDGE_ROTATE` (`PROT_ENC_TARGET=440`, `PROT_PIVOT=14`, `PROT_BRAKE=16`, freno fijo 400ms), y al terminar va directo a `FOLLOWING` (sin pasar por ningún estado de avance). El bloque de detección "en el aire" se restructuró para compartir el mismo timer `all_black_start_ms` entre ambos casos (picked-up vs perpendicular), diferenciados por `accel_moving`. Display: PGIRO. | Usuario pidió que el robot no pierda tiempo avanzando si el giro ya lo dejó sobre la línea, y que reconozca cruces perpendiculares (llegar de frente a una línea transversal) para alinearse solo en vez de tratarlo como "línea perdida" |
| 2026-07-01 | Core/Src/main.c | Revisión exhaustiva de todo el subsistema de línea/búsqueda de esta sesión (encoders, giros, clasificación centrado/extremo, avance post-giro, GIVEN_UP). Fix aplicado: `LOST_FWD`/`EDGE_FWD` (avance ciego post-giro) agregados a la lista de estados con corrección de yaw por giroscopio (antes solo tenían `LOST`/`SEARCHING`/`OBJ_PRE_REVERSE_HOLD`/`OBJ_HOLD`/`OBJ_WALL_FWD`) — sin esto, con `steering_adjustment=0` forzado, no había ninguna corrección de rumbo durante el avance y el robot podía irse curvando sin corregir. Hallazgo documentado pero NO corregido (fuera de alcance): el chequeo `f_fallen ||` dentro de `LOST_FWD`/`EDGE_FWD` para saltar a `GIVEN_UP` es código muerto — todo el switch de transiciones está anidado dentro de un `if (!f_fallen) {...}` más externo, así que ese chequeo nunca se cumple en la práctica (mismo patrón preexistente ya presente en `OBJ_HOLD` y `OBJ_WALL_APPROACH` desde antes de esta sesión, no introducido ahora). Impacto bajo: si el robot se cae, los motores igual se apagan por el guard exterior y el timeout de 3s lo manda a `GIVEN_UP` poco después de todos modos. Sin otros bugs encontrados: consistencia de enum/display/transiciones de todos los estados nuevos OK, timers compartidos (`lrot_settle_start_ms`, `obj_rot_*`) sin contaminación cruzada, `line_search_dir`/`EROT_DIR` no puede cambiar a mitad de giro, `last_detected_edge_only` maneja bien los casos borde (intersección ancha, arranque sin línea), `line_lost_ms` se resetea en el momento correcto antes de `LOST_FWD`/`EDGE_FWD` (el timeout de 3s cuenta desde ahí, no arrastra tiempo previo), `GIVEN_UP` sobrevive intacto a un ciclo de caída+recuperación. **Riesgo señalado sin confirmar**: `LROT_BRAKE`/`EROT_BRAKE=16.0` a torque fijo sostenido 400ms es bastante alto (casi el doble del 9.0 anterior) — dado el historial de esta sesión con freezes/resets sospechados de picos de corriente (después descartados a favor del bug de signo de encoders), vale la pena vigilar si reaparece algún freeze/reset en las próximas pruebas físicas. | Usuario pidió una revisión exhaustiva de todo el código tras la larga cadena de fixes de esta sesión |
| 2026-07-01 | Core/Src/main.c | Rediseño del freno final de `LOST_ROTATE`/`EDGE_ROTATE` a pedido explícito del usuario ("no hace falta frenar tan puntual"): eliminada toda la lógica basada en `rv` (velocidad real de rueda) para decidir la salida — sin sentido quedarse "pegado" en `GIRO` tratando de afinar una velocidad exacta. Ahora, una vez `enc_done`, aplica freno a **torque fijo LROT_BRAKE/EROT_BRAKE = 16.0** (subido de 9.0) durante un tiempo corto y acotado (`LROT_BRAKE_DURATION`/`EROT_BRAKE_DURATION = 400ms`) y sale sí o sí (el timeout de 3500-6000ms queda solo como colchón de seguridad ante `overshoot`). | El freno proporcional con piso (punto anterior) seguía sin convencer al usuario — se simplifica a frenar fuerte y avanzar, sin intentar precisión de velocidad |
| 2026-07-01 | Core/Src/main.c | Fix de "el giro dura demasiado / sigue en GIRO y se va hacia atrás": el freno proporcional a `rv` del punto anterior no tenía piso (`clamp(rv/1.0, 0, 1)`), así que al acercarse la velocidad a cero el freno también se acercaba a cero de forma asintótica — tardaba mucho en cruzar el umbral `vel_ok` (rv<0.5) y quedaba mucho tiempo "pegado" en `LOST_ROTATE`/`EDGE_ROTATE` con muy poco freno activo, tiempo durante el cual el balance normal podía hacerlo derivar hacia atrás. Agregado piso de `0.3` (`clamp(rv/1.0, 0.3, 1.0)`) en ambos giros — el freno siempre termina de frenar de verdad en vez de decaer indefinidamente. | El freno proporcional sin piso solucionó el overshoot pero introdujo el problema opuesto: convergencia asintótica demasiado lenta |
| 2026-07-01 | Core/Src/main.c | Dos cambios a pedido del usuario tras seguir viéndolo irse hacia atrás: (1) **`LOST_SETTLE` bypaseado**: `LOST_ROTATE` ahora transiciona directo a `LOST_FWD` apenas termina el giro por encoder, sin la pausa de estabilización (el usuario pidió explícitamente "no espere nada"). El estado `LOST_SETTLE` queda en el código sin usar por si hace falta reactivarlo. Solo se tocó el camino de 180°, `EDGE_ROTATE`→`EDGE_SETTLE` (90°/45°) no se tocó. (2) **Freno más intenso al perder la línea del todo**: nueva constante `LOST_BRAKE_BOOST=1.6`, aplicada solo en `LOST_BRAKE` y `EDGE_WAIT` (`brake_setpoint_target = clamp(ComputeBrakeSetpointTarget(...) * 1.6, ±BRAKE_TILT_MAX)`) para no alejarse tanto de donde se perdió la línea antes de empezar a buscar. No se tocó el freno estándar de `ComputeBrakeSetpointTarget` en general (afectaría también al balance común y reintroduciría la oscilación ya corregida). | Usuario pidió que el avance post-180° arranque inmediatamente sin esperar, y que el frenado inicial al perder la línea sea más intenso para no alejarse tanto de ella |
| 2026-07-01 | Core/Src/main.c | Fix de "se va hacia atrás después de girar": `LOST_FWD_SPEED_TARGET=0.45` seguía por debajo del pico de ruido de cuantización cuando tiquean las dos ruedas en el mismo ciclo (~0.63 m/s) — un solo pulso simultáneo se leía como sobrevelocidad y disparaba la ganancia de freno fuerte (`LOST_FWD_KP_BRAKE=10`), inclinando hacia atrás. Aplicado `apply_deadbandf(·, BRAKE_VEL_DEADBAND)` a la velocidad medida en `LOST_FWD` y `EDGE_FWD` antes de calcular el error, igual que ya se hacía en `ComputeBrakeSetpointTarget`. | Un solo pulso de encoder alcanzaba para cruzar el umbral de sobrevelocidad y frenar hacia atrás en vez de avanzar |
| 2026-07-01 | Core/Src/main.c | Giro de `EDGE_ROTATE` bajado de 60° a **45°** a pedido del usuario: `EROT_ABS_TARGET` 60→45, `EROT_ENC_TARGET` 293→220, `EROT_SLOWDOWN_DEG` 37→28 (escalados en proporción 45/90 respecto a los valores base calibrados para 90°). | Usuario pidió aún menos ángulo en el giro por pérdida en un extremo |
| 2026-07-01 | Core/Src/main.c | Giro de `EDGE_ROTATE` (perdida por un extremo) reducido de 90° a **60°** a pedido del usuario: `EROT_ABS_TARGET` 90→60, `EROT_ENC_TARGET` 440→293 y `EROT_SLOWDOWN_DEG` 55→37 (los tres escalados en proporción 60/90 respecto a los valores calibrados para 90°). El giro de 180° (`LOST_ROTATE`) no se tocó. | Usuario pidió que el giro por pérdida en un extremo sea un poco menos agresivo en ángulo |
| 2026-07-01 | Core/Src/main.c | **Fix del vaivén post-giro** ("se va para cualquier lado" en vez de quedarse quieto): el freno de la fase final de `LOST_ROTATE`/`EDGE_ROTATE` (una vez `enc_done`) aplicaba un torque diferencial **fijo** (`LROT_BRAKE`/`EROT_BRAKE`) sin relación con la velocidad real, hasta que `rv` bajara del umbral. Al bajar ese umbral de `2.0` a `0.5` rev/s (fix anterior), el freno fijo pasó a tener mucho más tiempo para actuar y se pasaba de largo, arrancando a girar en sentido contrario (vaivén, nunca se asienta). Fix: el freno ahora escala con `rv` (`brake_scale = clamp(rv/1.0, 0, 1)`), apagándose solo a medida que la rotación real decae, sin poder sobrepasarse. | El fix anterior (umbral de velocidad más estricto) expuso un problema latente: un freno de torque constante no es estable si tiene tiempo de sobra para actuar — necesita ser proporcional a la velocidad que está frenando |
| 2026-07-01 | Core/Src/main.c | Dos fixes más tras seguir probando: (1) **Reemplazado el enfoque de "pico reciente de line_error"** (con cualquier decay terminaba mal clasificado — el usuario confirmó que con 0.90 seguía haciendo siempre 90° aunque el medio fue el último en ver la línea) por una detección literal: `last_detected_edge_only` (bool) congela en el último ciclo con línea detectada si el ÚNICO sensor con señal fue un extremo (`w[0]` o `w[3]`) sin ningún soporte de los sensores del medio (`w[1]`/`w[2]` en cero). `line_was_centered_on_lost = !last_detected_edge_only`. Elimina toda la lógica de decay/umbral float, es un snapshot directo de qué sensor vio la línea por última vez, tal como lo describió el usuario. (2) **Fix de "no avanza, se queda quieto" en LOST_FWD/EDGE_FWD**: `balance_hold_active` solo se desactivaba con `line_detected` verdadero (bug ya visto una vez el 2026-05-18, ahora reaparecido en un contexto nuevo) — en el avance a ciegas post-giro `line_detected` es falso todo el tiempo, así que apenas el ángulo objetivo rampeaba desde 0° (error chico) el hold se activaba y silenciaba el PID de balance justo cuando debía empujar al robot hacia adelante. Condición ampliada para excluir también `LINE_STATE_LOST_FWD` y `LINE_STATE_EDGE_FWD`. | El pico con decay nunca encontró un punto de equilibrio correcto entre over/under-clasificar; el hold silenciando el PID en avance ciego es el mismo patrón de bug que ya se había visto y arreglado en otro contexto (seguimiento de línea normal) pero no se había extendido a los estados de búsqueda nuevos |
| 2026-07-01 | Core/Src/main.c | Dos fixes tras probar el punto anterior: (1) El decay del pico reciente de `line_error` (0.98/ciclo, memoria ~300-500ms) sobrecorrigió: quedaba "cargado" por el bamboleo normal de curvas pasadas y TODA pérdida se clasificaba como "por el extremo", el giro de 180° dejó de dispararse incluso viniendo centrado. Decay bajado a 0.90 (memoria ~100-150ms, suficiente para el desvanecimiento del sensor justo antes de perder la línea, sin arrastrar wobble de más atrás). (2) `EROT_DIR` (dirección del giro de 90°) invertido: `line_search_dir` en vez de `-line_search_dir` — el usuario confirmó en el robot que giraba hacia el lado contrario del esperado. | El primer ajuste del pico (0.98) fue demasiado agresivo y rompió el caso centrado; la dirección del giro de 90° estaba invertida respecto al comportamiento real observado |
| 2026-07-01 | Core/Src/main.c | Tres fixes tras reportes de "sigue sin frenar post-giro / no avanza en realidad / el giro de 90 nunca se dispara": (1) `vel_ok` en `LOST_ROTATE` y `EDGE_ROTATE` bajado de `rv<2.0` a `rv<0.5` rev/s (`p1e>=80→150ms`): 2.0 rps de rueda equivale a ~0.35 m/s, se consideraba "parado" con las ruedas todavía girando de verdad, y esa velocidad residual se arrastraba a SETTLE/FWD sin frenar de verdad. (2) `LSETTLE_TIMEOUT`/`ESETTLE_TIMEOUT` 1200→3000ms: eran un corte casi normal en vez de un colchón de seguridad, dejando avanzar con velocidad residual antes de estabilizar de verdad. (3) `LOST_FWD_SPEED_TARGET` 0.30→0.45 m/s: 0.30 caía DENTRO del piso de cuantización del encoder (~0.32-0.63, ver `BRAKE_VEL_DEADBAND`), así que el control P nunca encontraba un punto estable y peleaba entre acelerar y frenar a fondo, resultando en que el robot no avanzaba en absoluto. (4) **Bug del giro de 90° que nunca se disparaba**: `line_was_centered_on_lost` comparaba solo el último `line_error` puntual, pero justo antes de perder la línea del todo la señal del sensor extremo se desvanece y ese último valor vuelve a verse "centrado" aunque la pérdida real haya sido por un extremo. Nueva variable `line_error_peak_recent` (pico de `\|line_error\|` con decaimiento ~0.98/ciclo ≈ memoria de 0.3-0.5s), usada en el snapshot en vez del valor instantáneo. | El giro de 90° nunca se disparaba aunque la última lectura fuera claramente un sensor extremo (ADC1/ADC4); el de 180° no frenaba antes de avanzar; el avance post-giro se quedaba quieto en vez de avanzar |
| 2026-07-01 | Core/Src/main.c | Detección "en el aire" (`f_in_air`) ahora requiere que el acelerómetro esté variando constantemente además de los 4 sensores de línea en negro. Nueva métrica `accel_motion_f` (EMA de la suma de `\|Δax\|+\|Δay\|+\|Δaz\|` crudos entre ciclos, beta=0.30, calculada en `ControlStep10ms` justo después de leer el acelerómetro) y `ACCEL_MOTION_THRESHOLD=300.0` (punto de partida, ajustar en el robot real). Antes: al posar el robot sobre una zona sin línea (blanco totalmente), se disparaba `f_in_air` igual que si lo hubieran levantado con la mano, y los motores se detenían en lugar de seguir buscando la línea (LOST/EDGE). Ahora solo se activa si además hay movimiento sostenido del acelerómetro (= lo están manipulando); si está quieto sobre una zona sin línea, sigue el flujo normal de búsqueda. | El usuario quiere distinguir "lo levantaron con la mano" (debe detenerse) de "está parado sobre una zona blanca sin línea" (debe seguir buscando la línea) |
| 2026-07-01 | Core/Src/main.c | Fix de "no frena para nada, se acelera a fondo" post-giro + estado de reposo total. (1) **Control de avance reescrito**: el bang-bang viejo de `LOST_FWD`/`EDGE_FWD` (ángulo fijo `OBJ_WALL_APPROACH_ANGLE` hasta cruzar `OBJ_WALL_OVERSPEED_VEL=0.90`, recién ahí freno de `-1.0°`) nunca frenaba de verdad por debajo del umbral y podía acelerar sin límite. Reemplazado por un control P continuo (`LOST_FWD_SPEED_TARGET=0.30` m/s, `LOST_FWD_KP=6.0` acelerando, `LOST_FWD_KP_BRAKE=10.0` frenando, clamp `[-2.0, +2.0]°`) que reduce el ángulo suavemente a medida que se acerca al target y frena proporcionalmente si lo supera, en vez de un salto brusco entre 2 valores fijos. (2) **Nuevo estado `LINE_STATE_EDGE_SETTLE`**: el giro de 90° ahora también pasa por una pausa de estabilización antes de avanzar (idéntica a `LOST_SETTLE`), a pedido del usuario ("debería pasar a avanzar una vez que está más o menos quieto", aplicado a ambos giros). (3) **Nuevo estado terminal `LINE_STATE_GIVEN_UP`**: si `LOST_FWD` o `EDGE_FWD` no encuentran la línea en 3s (antes 10s, y antes volvían a `LINE_STATE_LOST` reintentando la búsqueda), ahora van a `GIVEN_UP` — reposo total (upright + freno de encoders, sin steering, sin timeout, sin reintento) hasta que se vuelva a detectar la línea (el usuario la repone a mano). Display: EESTAB/PARADO. | Usuario reportó que tras el giro de 180° el robot se aceleraba sin frenar en absoluto, y pidió que tras 3-4s sin encontrar la línea (probado primero 4s, bajado a 3s) el robot quede completamente quieto en vez de seguir reintentando la búsqueda indefinidamente |
| 2026-07-01 | Core/Src/main.c | Segunda pasada sobre la oscilación en balance común: el ajuste anterior de `BRAKE_VEL_DEADBAND` (0.05→0.12) seguía sin filtrar el ruido — calculado el piso de cuantización real de `velocity_est` (`ControlStep10ms`, delta de encoder cada 10ms, `ENC_CPR=28`): 1 count en una sola rueda ya da ≈0.32 m/s, ambas ruedas ≈0.63 m/s. Cualquier pulso aislado se leía como "movimiento real" y disparaba freno. `BRAKE_VEL_DEADBAND` subido a **0.35** (justo por encima del piso de 0.32) para filtrar el ruido de un solo pulso sin tapar una deriva sostenida real. | El deadband anterior (0.12) era un orden de magnitud menor que el piso de ruido real del sensor — no podía funcionar |
| 2026-07-01 | Core/Src/main.c | Reajuste del freno traslacional tras reporte de oscilación en balance común (BALANCE_ONLY): con `KV_BRAKE=2.0` (base, siempre activo) y `BRAKE_VEL_DEADBAND=0.05` (deadband chico), el ruido de cuantización de la nueva velocidad por encoders alimentaba una corrección de inclinación constante y el robot se balanceaba/oscilaba visiblemente parado y sin perturbación. Valores bajados a un punto intermedio entre el original y el primer ajuste agresivo: `KV_BRAKE` 2.0→0.8, `KV_BRAKE_STRONG` 8.0→6.0, `BRAKE_VEL_THRESHOLD` 0.6→1.0, `BRAKE_TILT_MAX`/`_MANUAL` 5.0→4.0, `BRAKE_VEL_DEADBAND` 0.05→0.12 (filtra el ruido de cuantización). Pendiente de confirmar en el robot que el balance común volvió a ser estable sin perder el freno más fuerte que corrigió la aceleración descontrolada en LOST/línea. | El primer ajuste (2026-07-01, punto "Frenado más agresivo") sobrecorrigió: sirvió para frenar el runaway pero introdujo oscilación en el caso normal de balance sin movimiento |
| 2026-07-01 | Core/Src/main.c | **Implementada la secuencia de recuperación "perdida por un extremo"** (el TODO del punto anterior). 3 estados nuevos: `LINE_STATE_EDGE_WAIT` (frena hasta velocidad baja o timeout 1500ms — la entrada a este estado ya ocurre ≥1s después de perder la línea, por el umbral existente en la salida de FOLLOWING), `LINE_STATE_EDGE_ROTATE` (gira 90° hacia el lado donde se vio la línea por última vez, misma lógica gz+encoder que `LOST_ROTATE` con `EROT_ENC_TARGET=440` counts = 90° —mitad de los 880 recalibrados para 180°—, dirección `EROT_DIR=-line_search_dir` ya que la convención es "izq=positivo" y hay que girar HACIA ese lado; no sale anticipadamente si ve la línea, igual que `LOST_ROTATE`), `LINE_STATE_EDGE_FWD` (avanza con velocidad controlada — mismo esquema de `LOST_FWD`, ángulo fijo + freno por overspeed — hasta encontrar la línea o timeout 10s). Disparo: en la salida de `LINE_STATE_FOLLOWING`, si `!line_was_centered_on_lost` va a `EDGE_WAIT` en lugar de `LINE_STATE_LOST` (que sigue yendo a la secuencia de 180°). Se reutiliza `line_search_dir` (existía pero no se usaba) para guardar el lado. Nueva variable `edge_wait_start_ms`. Display: EFRENA/EGIRO/EAVNZA. | Perder la línea en una curva necesita un giro de 90° hacia el lado por donde se fue la línea, no un giro ciego de 180° |
| 2026-07-01 | Core/Src/main.c | Dos cambios pedidos por el usuario sobre el giro de 180°: (1) **Ya no sale anticipadamente si detecta línea a mitad de giro** — se eliminó el `if(line_detected) break;` dentro de `LINE_STATE_LOST_ROTATE`; ahora el giro siempre completa los `LROT_ENC_TARGET` counts antes de re-evaluar la línea (en `LOST_SETTLE`/`LOST_FWD`). Evita cortar el giro a mitad de camino por una detección espuria/parcial mientras el robot gira rápido. (2) **El giro de 180° solo se ejecuta si el robot venía centrado sobre la línea** al perderla (no por los sensores extremos/curva). Nueva variable `line_was_centered_on_lost` (default 1), capturada en la transición FOLLOWING→LOST comparando `fabsf(line_error) < 0.15` (line_error retiene su último valor válido al perder la línea, no se resetea). La transición LOST_BRAKE→LOST_ROTATE ahora chequea esta variable: si es falso (se perdió por los extremos, ej. en curva), el robot se queda frenado y balanceado en el lugar en `LOST_BRAKE`, sin girar — **TODO: falta implementar la secuencia de recuperación para el caso "perdida por los extremos"**, pendiente de definir con el usuario. | El giro de 180° a ciegas no tiene sentido si la línea se perdió en una curva (por los sensores extremos); ese caso necesita otra maniobra distinta, todavía no implementada |
| 2026-07-01 | Core/Src/main.c | Nuevo estado `LINE_STATE_LOST_SETTLE` insertado entre `LOST_ROTATE` y `LOST_FWD`: al terminar el giro de 180°, el robot mantiene balance estático con freno de encoders (igual que `LOST_BRAKE`, `ComputeBrakeSetpointTarget(BALANCE_ONLY)`) durante un mínimo de 400ms, y sale cuando se cumple ese mínimo Y `|velocity_est_f|<0.10 m/s` Y el error de inclinación respecto al setpoint es `<3°`, o por timeout de seguridad de 1200ms. Si detecta la línea durante la espera, va directo a FOLLOWING. Nueva variable `lrot_settle_start_ms` (reseteada también en el bloque de caída). Display: "ESTAB". | Usuario pidió una pausa de estabilización fuerte después del giro, antes de arrancar a avanzar buscando la línea, en vez de pasar directo de girar a avanzar |
| 2026-07-01 | Core/Src/main.c | Giro 180° un poco más agresivo a pedido del usuario: `LROT_PIVOT` 12.0→14.0, `LROT_BRAKE` 8.0→9.0 (subido en proporción para no perder margen de frenado al final). Cambio moderado, no al nivel de 15 (valor que en sesiones previas se sospechó de causar glitches eléctricos, aunque esa hipótesis fue descartada después). | Usuario pidió giro "un poco más agresivo" pero no mucho más |
| 2026-07-01 | Core/Src/main.c | **Frenado más agresivo**, en dos frentes reportados por el usuario ("se acelera una locura" con y sin línea): (1) `ComputeBrakeSetpointTarget` (usado en LOST, LOST_BRAKE, HOLD, OBJ_BRAKE, WALL_*, MANUAL y el limitador global de velocidad en modo línea): `KV_BRAKE` 0.0→2.0 (antes CERO freno por debajo del umbral, el robot aceleraba libre), `KV_BRAKE_STRONG` 5.0→8.0, `BRAKE_VEL_THRESHOLD` 1.5→0.6 m/s (el freno fuerte arranca mucho antes), `BRAKE_TILT_MAX`/`BRAKE_TILT_MAX_MANUAL` 3.0→5.0° (más margen de inclinación para frenar, todavía lejos de los 60° de caída). (2) PI de velocidad del seguidor de línea (`LINE_STATE_FOLLOWING`): nueva ganancia `LINE_VEL_KP_BRAKE=7.0` (antes usaba la misma `LINE_VEL_KP=3.0` para acelerar y frenar) aplicada solo cuando `vel_error<0` (sobrevelocidad); clamp de `line_angle_cmd` ampliado de `[-1.5, LINE_ANGLE]` a `[-3.0, LINE_ANGLE]`; el guard posterior sobre `dynamic_setpoint_f` en LINE_FOLLOWING (que reclipeaba a -1.5°) también ampliado a -3.0° para no anular el freno más fuerte. Pendiente de validar en el robot que no overshootee hacia atrás con la ganancia más alta. | El freno era demasiado débil: sin línea, cero corrección hasta 1.5 m/s; con línea, mismo gain P para acelerar y frenar con clamp muy chico (-1.5°) |
| 2026-07-01 | Core/Src/main.c | **Fix crítico de signo en `SampleEncoders250us`**: `encoder_right/left += quad_delta[...]` cambiado a `-= quad_delta[...]`. La tabla estándar de decodificación de cuadratura (`quad_delta`) da la convención de signo opuesta a la que tenía el conteo viejo por EXTI (verificado a mano, transición por transición, para la misma secuencia física de giro: EXTI daba +1 por ciclo, la tabla da -1). Con el signo sin corregir, `velocity_est_f` quedaba invertido y `ComputeBrakeSetpointTarget()` (usado en casi todos los estados para frenar traslación y mantener el balance estático) empujaba el setpoint de inclinación en el sentido del movimiento en lugar de contrario — realimentación positiva en vez de negativa. | Usuario reportó tras la migración de encoders (punto anterior) que "se rompió toda la parte de balance" y el robot se aceleraba en vez de mantenerse balanceado — la velocidad de encoders alimenta el freno traslacional que sostiene el balance estático en reposo |
| 2026-06-30 | Core/Src/main.c | Agregado `line_error_trim_f = 0.05f`: offset constante sumado al centroide de línea (`line_error += line_error_trim_f`). Corrige deriva sistemática a la derecha causada por desplazamiento físico del array de sensores respecto al eje del robot. Ajustar empíricamente de a 0.05 hasta que el robot siga centrado. Positivo = corrige deriva derecha. | La calibración ADC_BASELINE no puede corregir un offset mecánico de montaje: cuando los sensores están centrados sobre la línea el error=0 y el PID no corrige, aunque el cuerpo esté desplazado |
| 2026-06-29 | Core/Src/main.c | LOST_ROTATE overshoot fix: `LROT_ENC_FRAC` 0.70→0.60 (frena al 60% del recorrido encoder, antes 70%); heading threshold fase 0→1 bajado de 80%→70% del target (126° en vez de 144°); `LROT_BRAKE` 5→8 (freno más fuerte en fase 1); `LROT_P1_MAX` 400→500ms. Robot hacía ~220° en vez de 180°. | La fase de freno empezaba muy tarde (144°) con freno suave (5 PWM), sin tiempo para frenar antes de los 180° |
| 2026-06-29 | Core/Src/main.c | LOST_FWD regulación de velocidad: setpoint ahora idéntico a WALL_FWD — si `velocity_est_f` supera `OBJ_WALL_OVERSPEED_VEL` (0.90 m/s), aplica `-OBJ_WALL_BRAKE_ANGLE` (-1.0°) en lugar de `OBJ_WALL_APPROACH_ANGLE` (+2.0°). Antes: ángulo fijo 2° sin ningún límite → robot se aceleraba hasta velocidad completa en AVNZA. | Robot se aceleraba excesivamente después del giro 180° al avanzar buscando la línea |
| 2026-06-29 | Core/Src/main.c | Control de velocidad mejorado en modo línea: (1) Floor de curvas 25%→10% (`LINE_SPEED_TARGET * 0.10f`): en curva cerrada el robot frena casi al mínimo. (2) Clamp de freno del PI de velocidad ampliado -1.0°→-1.5°. (3) Guard final `dynamic_setpoint_f` ampliado 1.0°→1.5° para permitir el freno extra. (4) Limitador global: si avanza >90% de `LINE_SPEED_TARGET` en cualquier sub-estado sin brake propio (LOST_FWD, LOST, WALL_APPROACH, etc.), llama a `ComputeBrakeSetpointTarget`. (5) Barras ADC pantalla 1 reducidas al 75% de altura. | Robot se aceleraba al perder la línea y en curvas; barras ADC tapaban zona superior del display |
| 2026-06-29 | Core/Src/main.c | `LINE_STATE_LOST_BRAKE` nuevo: al entrar a LOST, frena primero (upright + freno encoders) hasta `|velocity_est_f| < 0.15 m/s` o timeout 1500ms, luego pasa a LOST_ROTATE. `LROT_PIVOT` 12.0, `LROT_BRAKE` 5.0, `LROT_P0_MAX` 4000ms. Nombres de display actualizados (SIGUE/PERDI/FRENA/GIRO/AVNZA/ESPER/RETRO/STOP/ESQUIV/PAUSA/APRCH/PARED/GIRAP). | Giro más rápido y sin spike de corriente (robot ya está quieto al iniciar pivot) |
| 2026-06-29 | Core/Src/main.c | (reemplaza) `LROT_PIVOT` reducido de 15.0→5.0, `LROT_BRAKE` 5.0→3.0, `LROT_P0_MAX` 2400→4000ms en LOST_ROTATE. Al entrar a LOST_ROTATE desde FOLLOWING (robot en movimiento), el motor izquierdo puede revertir dirección súbitamente creando un spike de corriente → glitch VDD → CSS fires NMI → NVIC_SystemReset(). Con pivot=5 el spike es ~67% menor. Timeout alargado para compensar la velocidad reducida. | Freeze+reset durante el giro de 180° post-LOST (CSS/NMI por EMI) + giro más lento pedido por usuario |
| 2026-06-29 | Core/Src/main.c | Detección "en el aire": `f_in_air=1` si `adcAvg[0..3]` están todos por encima de `LINE_THRESHOLD` durante más de 2000ms en modo LINE_FOLLOWING. Cuando `f_in_air=1` → `motorRight=motorLeft=0` antes de `MotorControl()` → motores detenidos. Se limpia cuando cualquier sensor baja el umbral (robot toca superficie). Variables: `all_black_start_ms`, `f_in_air` (file-scope). | Robot quedaba en modo seguidor de línea con motores andando mientras lo sostenías en el aire |
| 2026-06-29 | Core/Src/main.c | Timeout FOLLOWING→LOST reducido de 2000→800ms. | Robot tardaba 2s en iniciar el giro de búsqueda después de perder la línea |
| 2026-06-29 | Core/Src/main.c | `KI_LINE` habilitado: 0.0→0.5. `ADC_BASELINE` calibrado sobre fondo blanco: {2000, 1550, 1370, 1730}. Sin baseline, el centroide estaba sesgado hacia el sensor con mayor lectura en blanco (s0=2000) → robot seguía la línea tirado a la derecha. | Sesgo derecho constante en seguidor de línea |
| 2026-06-29 | Core/Src/main.c | LOST → LOST_ROTATE inmediato: eliminadas condición de velocidad y timer de 3s. En cuanto el robot entra en LOST y no ve línea, el giro de 180° arranca en el siguiente ciclo (10ms). Antes: `velocity_est_f < 0.08f` era imposible de cumplir con 24 CPR (mínimo detectable 0.74 m/s), y el timer de 3s se reiniciaba por flickers de sensor. | Robot a veces nunca hacía el giro de búsqueda |
| 2026-06-27 | Core/Src/main.c | Bug auditoría — `obj_detect_ignore_until_ms` faltante en 4 transiciones: OBJ_HOLD→FOLLOWING por f_fallen, OBJ_WALL_APPROACH→FOLLOWING por f_fallen, OBJ_WALL_APPROACH→FOLLOWING por timeout, LOST_ROTATE→FOLLOWING por line_detected. Todas setean +5000ms ahora. | `obj_cnt` llega a 10 durante secuencias OBJ y re-dispara la evasión al volver a FOLLOWING si el objeto todavía es visible |
| 2026-06-27 | Core/Src/main.c | Bug auditoría — `balance_hold_active` podía activarse en LINE_STATE_LOST y LINE_STATE_SEARCHING: si el robot recibía un empujón leve que no superaba los umbrales de salida del hold (0.9°/10 dps), los motores quedaban apagados mientras el robot derivaba. Fix: agregar LOST y SEARCHING a la lista de exclusión del `balance_hold_active`. | Robot en LOST derivaba sin que los motores reaccionaran ante perturbaciones leves |
| 2026-06-27 | Core/Src/main.c | Bug "tonto post-evasión": `obj_cnt` llega a `OBJ_DETECT_DEBOUNCE_CNT=10` durante la secuencia OBJ y queda en 10 (capped). Al volver a FOLLOWING con objeto aún visible, `obj_cnt >= 10` dispara OBJ_PRE_REVERSE_HOLD inmediatamente → robot re-entra en evasión en loop. Fix: `obj_detect_ignore_until_ms = HAL_GetTick() + 5000U` en las transiciones OBJ_WALL_FWD→FOLLOWING, OBJ_WALL_TURN→FOLLOWING, y OBJ_WALL_APPROACH→FOLLOWING por timeout. | Robot seguía línea, esquivaba objeto, y al terminar la evasión se quedaba "tonto" sin seguir |
| 2026-06-27 | Core/Src/main.c | Display seguidor de línea: barras ADC 1-4 reducidas en altura (reservan 4px arriba para indicador). Indicador negro/blanco: cuadrado relleno=negro (ADC > LINE_THRESHOLD), borde=blanco. Las barras ADC 5-8 no cambian. | Las barras al máximo tapaban el indicador |
| 2026-06-27 | Core/Src/main.c | Bug crítico spin: causa raíz = floor 25% eliminado. Con `speed_factor=0` en curva cerrada (|line_error|≥0.45), `desired_vel=0` → PI da `line_angle_cmd=0` → `base_setpoint_target=0°` → robot equilibrado en 0° → `pwm_sat→0` → `steering_adjustment` residual produce spin puro. Fix: restaurado floor 25% (`desired_vel = max(0.25×LST, LST×sf)`), `brake_limit` fijo en -1.0° (V10). `balance_hold` restaurado a condición V10: excluido solo cuando `line_detected` (no en todo LINE_FOLLOWING). `steering_adjustment` conservado cuando `!line_detected` para ayudar al robot a volver a la línea. | Robot no seguía línea en absoluto tras quitar el floor — el setpoint caía a 0° en curvas y el robot giraba en su lugar sin avanzar |
| 2026-06-27 | Core/Src/main.c | Bug crítico LOST_ROTATE: `line_lost_ms` al entrar a LOST ya tenía 2000ms de antigüedad, por lo que `>500U` se cumplía inmediatamente → el robot giraba 180° al instante de perder la línea. Fix: nueva variable `line_lost_entered_ms` seteada al entrar a LOST; condición cambiada a `>3000U` desde ese timer. Ahora el robot espera 3s quieto en LOST antes de girar. | Robot "tonto" en la línea: entraba a LOST_ROTATE casi inmediatamente después de perder la línea, sin dar tiempo a recuperarla |
| 2026-06-27 | Core/Src/main.c | `OBJ_DETECT_THRESHOLD_VAL` bajado 3200→2000 y `OBJ_DETECT_DEBOUNCE_CNT` subido 3→10 (100 ms). Requiere que el objeto cause una caída de ADC más pronunciada Y que se mantenga 100 ms consecutivos antes de disparar la evasión. | Falsos positivos: entraba en modo esquive sin objeto real presente |
| 2026-06-27 | Core/Src/main.c | Pantalla 6 nueva (f_change_display==6): al entrar en cualquier estado OBJ_* del seguidor de línea, el display cambia automáticamente a pantalla de evasión que muestra el nombre del estado en Font_7x10 centrado (WAIT/REVERS/BRAKE/ROTATE/HOLD/WF_APR/WF_FWD/WF_TRN) + barra vertical ADC5 (sensor frontal) y barra vertical ADC7 (sensor lateral) con valor numérico. Al salir de OBJ vuelve a display 1. Detección vía `prev_line_state_disp >= LINE_STATE_OBJ_PRE_REVERSE_HOLD`. | Diagnóstico visual de cuándo el robot entra y sale del modo de evasión, y estado de los sensores de objeto |
| 2026-06-27 | Core/Src/main.c | Pantalla 1 (seguidor de línea) — panel derecho rediseñado: eliminado "LINE" del header superior (solo queda icono WiFi), eliminada línea VE (velocidad), sub-estado (FOLLOW/LOST/SEARCH/LROT/LFWD) ahora se muestra en Font_7x10 centrado (en vez de Font_5x7). Spinner reducido (spokes ±3) y movido a y=59. | Más espacio y legibilidad para el estado actual del seguidor de línea |
| 2026-06-26 | Core/Src/stm32f4xx_it.c | `NMI_Handler`, `BusFault_Handler`, `MemManage_Handler`, `UsageFault_Handler`: agregado `NVIC_SystemReset()` antes del `while(1)`. Antes quedaban en loop infinito → IWDG expiraba después de 2s de freeze. Ahora cualquier fault produce reset inmediato. Causa probable durante acción de motores: caída VDD → glitch HSE → CSS dispara NMI, o EMI PWM → acceso DMA inválido → BusFault. | HardFault ya tenía reset; los otros 4 no → 2s de robot congelado con motores fijos. |
| 2026-06-27 | Core/Src/main.c | `LINE_STATE_LOST_FWD` (nuevo estado): después del giro de 180° (LOST_ROTATE), avanza con ángulo fijo 2° hasta encontrar la línea → FOLLOWING. Timeout 10s de seguridad → LOST. Display: "LFWD". | Post-180° el robot no está sobre la línea; volver a LOST inmediatamente disparaba otro ciclo LOST→ROTATE indefinidamente. |
| 2026-06-26 | Core/Src/main.c | `LINE_STATE_LOST_ROTATE` (nuevo estado): cuando el robot pierde la línea y la velocidad de encoders cae por debajo de 0.08 m/s (mínimo 500ms en LOST), hace un giro de 180° derecha con la misma lógica gz+encoder que MANUAL/OBJ_ROTATE. Si detecta la línea durante el giro → FOLLOWING inmediato. Al terminar el giro → vuelve a LOST para re-buscar. Parámetros: ENC_TARGET=760 (380×2), PIVOT=15, BRAKE=5, SLOWDOWN=55°, P0_MAX=2400ms. Display: "LROT". | Robot se quedaba en LOST indefinidamente sin buscar activamente la línea. |
| 2026-06-26 | Core/Src/main.c | `LINE_STATE_OBJ_BRAKE`: `PRE_ROTATE_MS` 1000→2000ms. Pausa más larga post-reversa para que el robot se estabilice antes del giro. | El robot entraba al giro todavía con algo de movimiento residual. |
| 2026-06-26 | Core/Src/main.c | `LINE_STATE_OBJ_ROTATE`: parámetros igualados a MANUAL: `ENC_TARGET` 325→380, `PIVOT` 8→15, `BRAKE` 3→5, `SLOWDOWN_DEG` 35→55, `P0_MAX` 3000→1200ms, `P1_MAX` 500→300ms, clamp motores ±30→±60. Heading compuesto gz+encoder, slowdown ramp, overshoot detection. | Con PIVOT=8 el giro era demasiado suave y no llegaba a 90°. Parámetros idénticos a MANUAL garantizan comportamiento equivalente. |
| 2026-06-26 | Core/Src/main.c | `LINE_STATE_OBJ_REVERSE`: `rev_target_counts` 10→200 (~10 cm). Retroceso re-habilitado para dar espacio antes del giro y no chocar el objeto con la carrocería. | 10 counts era prácticamente nada. |
| 2026-06-25 | Core/Src/main.c | Fix giro izquierda 90°: (1) `phase0_max_ms` reducido 1000→600 ms para 90°: con gz≈0 el fallback ya no gira 1 s entero. (2) `gz_settled`: `gz<12` en fase 1 solo sale DESPUÉS de 300 ms mínimos (antes salía en la 1ra iteración sin frenar). `phase1_max_ms` 400→500 ms. | gz no acumula bien CCW → fase 0 duraba 1 s → fase 1 salía a los 0 ms → 370°+ de inercia sin frenar. |
| 2026-06-25 | Core/Src/main.c | Fix overshoot 180°: `enc_exit_frac = 0.85 - max(0,(angle-90)/90)*0.15` → 90°=0.85 sin cambio, 180°=0.70 entra al freno antes. Auto-ciclo extendido a 3 pasos: 90°R → 90°L → 180°R (% 3). `MANUAL_ROT_ENC_TARGET` calibrado empíricamente a 380 counts/90°. | 180° acumulaba el doble de momentum; entrar a fase 1 al 70% da tiempo de freno suficiente sin afectar los giros de 90°. |
| 2026-06-25 | Core/Src/main.c | Encoder feedback para heading en rotación MANUAL: `enc_heading_deg = avg(|dr|+|dl|)/2 × (90/325)` calculado en cada ciclo. Heading compuesto = `max(gz, enc)`. Usado en: (1) transición fase 0→1 al 55% — ahora funciona para giro izquierda donde gz≈0; (2) slowdown usa `abs_remaining = abs_target - abs_heading` con heading compuesto; (3) salida anticipada de fase 1 cuando `enc >= 95%` del target (mínimo 60 ms). 325 counts = 90° confirmado empíricamente desde OBJ_ROTATE. | Giro izquierda no tenía feedback angular (gz≈0 en CCW) y dependía solo del tiempo (500 ms fallback). Encoders dan precisión simétrica para ambos sentidos. |
| 2026-06-25 | Core/Src/main.c | Fix "va y vuelve" en giro automático MANUAL: `MANUAL_ROT_BRAKE_POWER` 15→5 (freno suave, diferencial 10 PWM en lugar de 30), `phase0_max_ms` 600→400 ms (menos momentum acumulado), `phase1_max_ms` 500→250 ms, mínimo gz_settled 300→120 ms. Causa raíz: con BRAKE=15 y 500 ms de contra-rotación, la fase de freno acumulaba suficiente momentum inverso para revertir el giro completo → el robot volvía a ~0° dando la ilusión de un segundo giro en sentido contrario. Con freno más suave el robot desacelera y se queda en la nueva orientación. | Síntoma: robot giraba derecha y volvía izquierda (overshoot de fase 1), no un segundo trigger del auto-ciclo. |
| 2026-06-25 | Core/Src/main.c | Fix colapso post-giro derecha: fase 1 ahora usa `-(pwm_sat ∓ dir×BRAKE)` en lugar de `±dir×BRAKE` puro. La diferencia entre ambos motores sigue siendo 2×BRAKE_POWER (frena inercia rotacional, constante independiente de pwm_sat), mientras que el modo común `pwm_sat` mantiene el balance PID activo durante los 500 ms de freno. | Con `±dir×BRAKE` puro (sin pwm_sat), el PID de balance calculaba pero no se aplicaba → robot se inclinaba durante el freno → caída al terminar el giro. |
| 2026-06-25 | Core/Src/main.c | Fix secuencia automática de giros: `manual_auto_rot_last_ms` (elapsed desde último trigger) reemplazado por `manual_seq_next_ms` (deadline absoluto). La pausa de 2.5 s ahora se setea como `HAL_GetTick() + 2500` exactamente cuando el giro TERMINA (en ambas ramas de salida: fase 1 normal y timeout). El trigger de respaldo al disparar se setea a `+10000` para que nunca compita con el deadline real. Al entrar a MANUAL_CONTROL se setea `+2500` para dar pausa inicial. | Con la variable elapsed, la pausa se podía medir desde el trigger (no desde el fin), acortando efectivamente la pausa visible entre giros. |
| 2026-06-23 | Core/Src/main.c | Fix giro izquierda modo MANUAL (3ra iteración): fase 1 ahora usa freno puro sin `pwm_sat`. Causa raíz: en la fórmula `motorR = -(pwm_sat + dir*pivot)`, cuando `pwm_sat` es grande, ambos motores terminan con el mismo signo → TIM3/TIM4 ambos en modo "dirección forward" → efecto traslacional puro, sin diferencial de rotación → freno inefectivo. Con `motorR = dir*BRAKE, motorL = -dir*BRAKE` el freno es independiente del balance y siempre produce el diferencial correcto. El robot puede inclinarse levemente durante el freno pero la fase 1 es corta (<1s). |
| 2026-06-23 | Core/Src/main.c | Fix giro izquierda modo MANUAL: (1) `MANUAL_ROT_SLOWDOWN_DEG` 35°→55° — el slowdown nunca se activaba porque la transición fase 0→1 dispara a 49.5° (55% de 90°) pero el slowdown empieza a 90-35=55°, es decir DESPUÉS de la transición. Con 55° empieza a 35°, dentro de la zona activa. (2) Condición de salida de fase 1: `crossed_zero` corregido a `reversed` (la versión anterior era inversa y salía mientras el robot AÚN giraba en la dirección original). Ahora: `reversed = (gz_dps * gz_sign < 0)` = verdadero cuando gz realmente cambió de signo (freno sobrecompensó). Overshoot guard bajado a 120% (era 150%). Salida: `|gz|<12 || overshoot || (reversed && |gz|<40)`. (3) Comentario del ciclo auto actualizado (no hay 180° en el ciclo actual). | `crossed_zero` invertido causaba: derecha=queda corto (salía con robot AÚN girando CW), izquierda=hace 180° (overshoot a 135° + inercia) |
| 2026-06-02 | Core/Src/main.c | Fix sobrepaso de giro preciso: `MANUAL_ROT_PIVOT_POWER` 12→7, `MANUAL_ROT_SLOWDOWN_DEG` 15→35°, mínimo de potencia 0.3→0.0. Agregado `MANUAL_ROT_BRAKE_POWER=5` y `manual_rot_phase`: fase 0=avance (para al 72% del ángulo), fase 1=contra-pivot activo hasta que `gz < 15 dps` y `heading ≤ target + 3°`. Fix balance MANUAL≡BALANCE_ONLY: `pwm_limit` MANUAL 55→100 (anti-windup ahora idéntico), `BRAKE_TILT_STEP_MAN` 0.3→0.5 | Sobrepaso excesivo en giro de 90°; balance MANUAL diferente a BALANCE_ONLY en anti-windup e integral |
| 2026-06-02 | Core/Src/main.c | Giro automático en modo MANUAL: cada 5 s (desde entrada al modo o desde fin del giro anterior) dispara `manual_rot_target_deg=90°` + `manual_rot_trigger=1` si no hay giro activo ni caída. Variable `manual_auto_rot_last_ms` reseteada al entrar en MANUAL y al finalizar cada giro. | Demo/prueba de giros precisos sin intervención desde Qt |
| 2026-06-02 | Core/Inc/UNER.h, Core/Src/UNER.c, Core/Src/main.c | Giro preciso en modo MANUAL: 5 comandos UNER nuevos (`ROTATE_90_RIGHT=0xD5`, `ROTATE_90_LEFT=0xD6`, `ROTATE_180_RIGHT=0xD7`, `ROTATE_180_LEFT=0xD8`, `ROTATE_CUSTOM=0xD9` con float 4 bytes). Sensor primario: giroscopio gz integrado a 100 Hz. Slowdown en los últimos `MANUAL_ROT_SLOWDOWN_DEG=15°` (baja de 100% a 30% de `MANUAL_ROT_PIVOT_POWER=12`). Timeout dinámico proporcional al ángulo. Registro: `UNER_RegisterRotationCmd(&manual_rot_target_deg, &manual_rot_trigger)`. Qt debe informar mismo protocolo | Necesidad de giros precisos desde Qt sin tocar el seguidor de línea |
| 2026-06-02 | Core/Src/main.c | Detección de "puesto en el piso" en modo LINE: si los 4 sensores de línea (`adcAvg[0..3]`) estaban todos por encima de `LINE_THRESHOLD` (robot en el aire) y alguno baja, se activa `obj_detect_ignore_until_ms = HAL_GetTick() + 3000` para ignorar obstáculos 3s. Evita que la mano al soltar el robot dispare la evasión. Variable `prev_all_line_black` (file-scope, init=1) lleva el estado del ciclo anterior | La mano bajo los sensores al soltar el robot disparaba obj_detect constantemente |
| 2026-06-01 | Core/Src/main.c, Core/Src/stm32f4xx_it.c | ADC CH5–CH8 (`adcAvg[4..7]`, sensores de objeto) cambiados a `ADC_SAMPLETIME_144CYCLES` (antes 15 ciclos) para mayor sensibilidad/rango de detección de objetos. Fix crítico 1: CubeMX reseteó prioridades DMA1_S0/S1, DMA2_S0 y EXTI15_10 a 0 → I2C DMA freeze; corregido forzando prioridades en USER CODE BEGIN 2. Fix crítico 2: CubeMX borró `EXTI9_5_IRQHandler` completo y mutiló `EXTI15_10_IRQHandler` (sin manejo de PB13/14/15) → CPU en Default_Handler loop infinito al primer pulso de encoder; restaurados manualmente en stm32f4xx_it.c | Tras cualquier regeneración de CubeMX: `git diff Core/Src/stm32f4xx_it.c` primero |
| 2026-05-28 | Core/Src/main.c | `OBJ_WALL_FWD` dejó de regular velocidad fina con PI: ahora usa `OBJ_WALL_FWD_ANGLE=4.5f` constante mientras `ADC7` está entre `500` y `3750`, frena solo si `velocity_est_f` supera `OBJ_WALL_OVERSPEED_VEL=0.90f`, y aplica corrección yaw con giroscopio igual que hold/lost. `OBJ_WALL_TOO_CLOSE_THOLD` ajustado a `500.0f` | Los encoders son demasiado cuantizados para controlar `0.25 m/s`; el PI alternaba avance/freno y el robot bamboleaba sin avanzar recto |
| 2026-05-28 | Core/Src/main.c | `OBJ_WALL_FWD_ANGLE` aumentado de `2.5f` a `4.5f` | El ángulo máximo anterior era demasiado chico y el robot no avanzaba durante `OBJ_WALL_FWD` |
| 2026-05-27 | Core/Src/main.c | `OBJ_DETECT_THRESHOLD_VAL` cambiado de `2000.0f` → `3200.0f`. El sensor detecta objeto cuando `adcAvg[4..7] < 3200`. Se alargó la distancia de sensado cambiando resistencias en hardware, por lo que el ADC ahora satura antes y el umbral viejo quedaba corto | Hardware actualizado: sensores de objeto ahora responden a mayor distancia |
| 2026-05-27 | Core/Src/main.c | `rev_target_counts` en `LINE_STATE_OBJ_REVERSE` reducido de `300` → `150` counts (exactamente la mitad). Reduce el recorrido de retroceso antes del giro de evasión | Recorrido anterior era excesivo para la nueva distancia de detección; la mitad es suficiente para despejar el obstáculo |
| 2026-05-27 | Core/Src/main.c | OBJ_REVERSE y OBJ_BRAKE deshabilitados temporalmente: `OBJ_PRE_REVERSE_HOLD` ahora transiciona directo a `OBJ_ROTATE` en lugar de `OBJ_REVERSE`. Cadena activa: FOLLOWING → WAIT(2s) → ROTATE → HOLD(2s) → ARC → FOLLOWING. Los estados OBJ_REVERSE y OBJ_BRAKE permanecen en el enum y en el código pero nunca se alcanzan | Simplificar la evasión para probar sin reversa; se puede re-habilitar en una línea |
| 2026-05-27 | Core/Src/main.c | `OBJ_ARC_DIFF_TARGET` corregido de `-0.5f` → `+0.5f`. Validación empírica mostró que la convención `speed_right_rps_s - speed_left_rps_s` tiene signo opuesto al esperado en esta plataforma: positivo produce giro a la izquierda, negativo a la derecha. Comentarios en el código actualizados para reflejar la convención real | El robot giraba a la derecha con `-0.5f`; la corrección de signo lo invierte a la izquierda |
| 2026-05-27 | Core/Src/main.c | `OBJ_ARC_STEER` (open-loop, constante) reemplazado por PI cerrado sobre diferencial de encoders. Nuevo parámetro tunable `OBJ_ARC_DIFF_TARGET` [rps] = diferencial deseado `(speed_right - speed_left)`; positivo = gira izquierda. Ganancias: `OBJ_ARC_KP=15.0`, `OBJ_ARC_KI=3.0`, saturación ±`OBJ_ARC_STEER_MAX=30`. Anti-windup: si satura, deshace el último paso de integral. Variable de estado `obj_arc_steer_int` file-scope, reseteada en caída y al salir del estado | Open-loop era inconsistente: el radio del arco variaba con la batería y asimetrías mecánicas; el PI lo regula midiendo la diferencia real de encoders |
| 2026-05-27 | Core/Src/main.c | Agregado `LINE_STATE_OBJ_ARC`: avanza en arco con PI de diferencial de encoders hasta re-detectar línea → vuelve a FOLLOWING con integrales limpios. OBJ_HOLD modificado: ahora tiene timer `OBJ_HOLD_DURATION_MS=2000ms` y sale a OBJ_ARC (antes se quedaba indefinidamente). `obj_hold_start_ms` file-scope, reseteado en caída. Parámetros: `OBJ_ARC_DIFF_TARGET=0.5f` (izquierda), `OBJ_ARC_ANGLE=1.0°`. Cadena completa: FOLLOWING → WAIT → REVERS → BRAKE → ROTATE → HOLD(2s) → ARC → FOLLOWING | El robot se quedaba en HOLD para siempre sin reincorporarse a la línea; el arco completa la evasión y re-engancha el seguidor |
| 2026-05-27 | Core/Src/main.c | `obj_rev_initialized` ahora se resetea a 0 en el bloque de detección de caída (f_fallen=1). Sin este fix, si el robot caía durante OBJ_REVERSE y se recuperaba, el snapshot de encoder era viejo y `rev_counts >= 300` inmediatamente → saltaba a OBJ_BRAKE sin retroceder. Mismo patrón del bug ya corregido en OBJ_ROTATE | Bug: encoder snapshot stale post-caída durante OBJ_REVERSE |
| 2026-05-27 | Core/Src/main.c | `brake_start_ms` en `OBJ_BRAKE` promovido de `static` local a variable de archivo `obj_brake_start_ms`. Reseteado a 0 en: detección de caída, transición OBJ_REVERSE→OBJ_BRAKE, y al salir del estado. Sin fix: si el robot caía durante OBJ_BRAKE, al recuperarse el tiempo transcurrido superaba el timeout (1500ms) y saltaba a OBJ_ROTATE sin frenar | Bug idéntico al que afectaba a OBJ_ROTATE con `rot_initialized` |
| 2026-05-27 | Core/Src/main.c | `obj_rot_initialized` y `obj_rot_start_ms` ahora se resetean en el bloque de caída. Sin fix: si el robot caía durante OBJ_ROTATE, los counts acumulados durante el rodamiento sumaban al snapshot previo → el conteo podía superar target_counts=325 inmediatamente al recuperarse → saltaba a OBJ_HOLD sin haber girado | Mismo patrón de bug: snapshot de encoder stale post-caída en OBJ_ROTATE |
| 2026-05-27 | Core/Src/main.c | `KV_brake_value` (registrado con UNER para el slider "KV" en Qt) ahora se usa en `ComputeBrakeSetpointTarget` en lugar de la constante compilada `KV_BRAKE_STRONG`. Antes el slider de Qt enviaba valores a `KV_brake_value` pero la función de freno ignoraba la variable y usaba siempre 5.0f | Bug funcional: el ajuste de freno desde Qt no tenía efecto en runtime |
| 2026-05-27 | Core/Src/main.c | `line_error_f_d` agregado al bloque de caída y al bloque de recuperación (antes solo se limpiaba en las transiciones de estado). Sin esto, al retomar OBJ_REVERSE después de una caída, el término derivado del PID de línea tenía un spike de hasta ±16 en el primer ciclo | Previene spike de steering en primer ciclo post-caída en OBJ_REVERSE |
| 2026-05-27 | Core/Src/main.c | `line_obj_rev_vel_integral` y `line_error_f_d` agregados al bloque de recuperación (f_fallen→0). El bloque de caída ya los limpiaba, pero durante los ciclos de caída el integral se re-acumulaba (el bloque de velocidad corre antes de la detección de caída). Al recuperarse, ahora se garantiza que el integral arranca desde 0 | Consistencia: garantiza estado limpio al arrancar de recuperación, no solo en el ciclo de caída |
| 2026-05-27 | Core/Src/main.c | `obj_hold_initialized` eliminado: era una variable de archivo declarada (línea ~379) y reseteada en OBJ_ROTATE→OBJ_HOLD, pero nunca leída ni escrita en el case OBJ_HOLD — dead code puro | Limpieza: la variable fue parte de un hold personalizado que se simplificó y quedó sin uso |
| 2026-05-27 | Core/Src/main.c | Agregado `LINE_STATE_OBJ_ARC`: avanza en arco con `OBJ_ARC_STEER=20` y `OBJ_ARC_ANGLE=1.2°` hasta re-detectar línea → vuelve a FOLLOWING. OBJ_HOLD ahora tiene timer `OBJ_HOLD_DURATION_MS=2000ms` y sale a OBJ_ARC. `obj_hold_start_ms` file-scope, reseteado en caída. Cadena completa: FOLLOWING→WAIT→REVERS→BRAKE→ROTATE→HOLD→ARC→FOLLOWING | El robot se quedaba en HOLD indefinidamente sin reincorporarse a la línea |
| 2026-05-27 | Core/Src/main.c | Bloque de recuperación (f_fallen→0): corregida indentación extra en `line_integral = 0.0f` y variables reordenadas para consistencia visual con el bloque de caída | Cosmético |
| 2026-05-27 | Core/Src/main.c | `OBJ_WALL_FWD`: PI de velocidad eliminado (era la causa del "no avanza" — el robot en WALL_TURN con PIVOT_POWER=5 no salía del sitio, y el PI en WALL_FWD nunca llegaba a empujar). Reemplazado por ángulo fijo `OBJ_WALL_FWD_ANGLE=5.5°`. El PID de balance regula la inclinación directamente. Eliminados defines `OBJ_WALL_SPEED_TARGET`, `OBJ_WALL_VEL_KP`, `OBJ_WALL_VEL_KI`, `OBJ_WALL_VEL_I_MAX`. `obj_wall_vel_int` mantenida pero solo se resetea. | Robot se quedaba completamente detenido en WF_FWD |
| 2026-05-27 | Core/Src/main.c | `OBJ_WALL_PIVOT_POWER` aumentado de 5.0 → 20.0. Con 5.0 PWM el diferencial era insuficiente para rotar (por debajo del umbral efectivo de los motores). Con 20 el robot pivotea con claridad. | WALL_TURN no giraba — potencia insuficiente |
| 2026-05-27 | Core/Src/main.c | Detección "demasiado cerca" en WALL_FWD y WALL_TURN: si `adcAvg[6] < OBJ_WALL_TOO_CLOSE_THOLD=300`, pivotea derecha (mismo signo que OBJ_ROTATE) en lugar de avanzar/izquierda. Prioridad: `line_detected` > `too_close` > `!wall_visible` > avance. Nuevo define `OBJ_WALL_TOO_CLOSE_THOLD=300.0f`. | Evitar que el robot choque con el obstáculo cuando se acerca demasiado |
| 2026-05-27 | Core/Src/main.c | `steering_adjustment = 0.0f` explícito en rama de avance de WALL_FWD (cuando wall_visible y no too_close). Antes: valor residual de OBJ_HOLD podía causar desvío no deseado. | Prevenir desvío lateral al avanzar junto al objeto |
| 2026-05-27 | Core/Src/main.c | `obj_wall_vel_int = 0.0f` agregado en transición OBJ_HOLD→WALL_FWD. | Estado limpio al entrar en wall-following |
| 2026-05-27 | Core/Src/main.c | `OBJ_WALL_PIVOT_POWER` ajustado de 20.0 → 8.0 (20 era demasiado agresivo). `OBJ_WALL_LINE_IGNORE_MS=3000` agregado: al entrar a WALL_FWD se ignora `line_detected` durante 3s para no volver a FOLLOWING al pasar sobre la línea original. `obj_wall_fwd_start_ms` nuevo file-scope, inicializado al entrar desde OBJ_HOLD o WALL_TURN, reseteado al salir. | Robot volvía a FOLLOWING inmediatamente al cruzar la línea y el pivot era excesivo |
| 2026-05-26 | Core/Src/main.c | Detección de objetos re-habilitada: `obj_now` restaurado a `adcAvg[4..7] < OBJ_DETECT_THRESHOLD_f` (estaba forzado a 0). Cadena de evasión FOLLOWING→OBJ_REVERSE→OBJ_BRAKE→OBJ_ROTATE→OBJ_HOLD activa nuevamente | Se había deshabilitado para trabajar solo en seguimiento de línea |
| 2026-05-26 | Core/Src/main.c | OBJ_REVERSE: cuando no hay línea visible, `steering_adjustment` ahora usa feedback de encoders `(rev_dr - rev_dl) × REV_ENC_KP(0.05)` en lugar de 0. Compensa asimetría mecánica de la rueda izquierda que retrocede menos que la derecha | Reversa torcida: motor izquierdo tiene mayor resistencia en ese sentido |
| 2026-05-26 | Core/Src/main.c | `OBJ_REVERSE` reemplaza el setpoint fijo `-1.5°` por PI de velocidad hacia atrás (`LINE_OBJ_REV_*`) y usa PID de línea con error invertido, limitado por `LINE_OBJ_REV_STEER_MAX`. El clamp negativo de modo línea permite `-LINE_OBJ_REV_TILT_MAX` solo en reversa por obstáculo | Mantenerse sobre la línea al retroceder ante obstáculos, inclinando hacia atrás como el seguidor inclina hacia adelante |
| 2026-05-26 | Core/Src/main.c | Agregado `LINE_STATE_OBJ_PRE_REVERSE_HOLD`: al detectar obstáculo espera `OBJ_PRE_REVERSE_HOLD_MS=2000` en balance estático con freno por encoders antes de pasar a `OBJ_REVERSE`. Display muestra `WAIT` durante esa pausa | Suavizar la transición entre seguimiento de línea y marcha atrás |
| 2026-05-04 | CLAUDE.md | Lectura inicial completa del proyecto: hardware, periféricos, PID, protocolo UNER, arquitectura real de archivos | Primera documentación desde el código fuente |
| 2026-05-07 | Core/Src/main.c | Agregado soporte de encoders por cuadratura vía EXTI: variables `encoder_right/left`, GPIO init PA8/PB13/PB14/PB15, callbacks EXTI y cálculo de `speed_right/left_rps` en `ControlStep10ms` | Medición real de velocidad de ruedas con encoders físicos |
| 2026-05-07 | Core/Src/main.c | `velocity_est`/`velocity_est_f` ahora alimentados desde encoders (`vel_enc = promedio(speed_right_rps, speed_left_rps) × ENC_VEL_SCALE`). Eliminadas las 3 llamadas a `UpdateVelocityEstimate` (accel+gyro). Display "VE:" ya mostraba `velocity_est_f` sin cambios. `ENC_VEL_SCALE=0.17750f` (radio 2.825 cm). | Velocidad real de ruedas en lugar de estimación por fusión inercial |
| 2026-05-07 | Core/Src/main.c | Agregada infraestructura para steering de lazo cerrado por encoders: `ComputeSteeringPID()`, `SteeringPID_Reset()`, flag `steer_pid_enabled`. Por defecto deshabilitado (=0, comportamiento idéntico al anterior con corrección por gyro Z). Activar con `steer_pid_enabled=1` y ajustar `STEER_KP/KI/KD`. | Base para control de dirección preciso usando diferencia de velocidades de encoders |
| 2026-05-08 | Core/Src/main.c | Motor izquierdo invertido: negado en las dos llamadas a `MotorControl` (líneas con `motorLeftVelocity`). Los encoders también tenían signo invertido: `vel_enc = -((speed_r + speed_l) * 0.5f) * ENC_VEL_SCALE` | Motores nuevos de 1000 RPM tenían polaridad opuesta al lado izquierdo |
| 2026-05-08 | Core/Src/stm32f4xx_it.c | Agregado `EXTI9_5_IRQHandler` (faltaba → CPU caía en Default_Handler). Corregido `EXTI15_10_IRQHandler`: limpia flags de líneas no usadas con `EXTI->PR` (write-1-to-clear) y llama HAL para PB12, PB13, PB14, PB15. Esto elimina el re-ingreso infinito al handler que trababa el CPU | Fix definitivo del HardFault por interrupt storm de encoders |
| 2026-05-08 | Core/Src/main.c | Masking anti-storm en callback de encoders: tras cada pulso se enmascara el EXTI (`EXTI->IMR &= ~pin`). TIM5 (2ms) re-habilita todos los pines del encoder. Limita a 500 Hz max por canal | Previene freeze por rafagas de interrupciones del encoder |
| 2026-05-08 | Core/Src/main.c | Encoder 4x quadrature: PB13 y PB15 pasan de input mudo a EXTI RISING+FALLING. Callback maneja 4 canales: A con `(a==b)`, B con `(a!=b)`. `ENC_CPR` 12→24. TIM5 re-habilita los 4 pines | Duplica resolución efectiva: de 12 a 24 conteos/rev |
| 2026-05-08 | Core/Src/main.c | `KV_BRAKE` puesto en 0.0f (base velocidad baja). `KV_BRAKE_STRONG` ajustado a 5.0f (era 8.0f). `KV_brake_value` inicializado a `KV_BRAKE_STRONG`. Slider "KV" en Qt controla `KV_brake_value` en runtime | Adaptación a motores 1000 RPM (25% más rápidos); freno fuerte funciona bien con velocidad de encoders |
| 2026-05-08 | Core/Src/main.c | Seguidor de línea mejorado: (1) PI de velocidad externo (`LINE_VEL_KP=2.5`, `LINE_VEL_KI=0.8`) reemplaza `line_speed_scale` + `KV_LINE_BRAKE`. (2) Inner steering PI con encoder feedback (`LINE_STEER_FB_KP=5.0`, `LINE_STEER_ENC_SCALE=0.12`) reemplaza steering open-loop. (3) Reducción cuadrática de velocidad con `|line_error|` reemplaza `forward_factor` lineal. Variables `speed_right/left_rps_s` expuestas como statics. `LINE_SPEED_TARGET=0.25 m/s` ajustable en runtime. | Control fluido y robusto: la velocidad y el giro se regulan por realimentación real, no heurísticas abiertas |
| 2026-05-09 | Core/Src/main.c | `LINE_ANGLE` cambiado de 0.5° → 3.0°. Bug: 0.5° coincidía con `BALANCE_HOLD_ENTER_ANGLE_DEG=0.5°`, activando hold casi siempre → `pwm_sat≈0` → el steering (5–20 PWM) superaba el avance → ruedas retrocedían. Con 3.0° el `pwm_sat≈12`, suficiente para superar el steering en línea recta. | Fix de "cuesta ganar ángulo para avanzar" y "cuando ve la línea se va hacia atrás" |
| 2026-05-09 | Core/Src/main.c | `desired_vel` en el PI de velocidad del seguidor de línea ahora tiene un mínimo del 25% de `LINE_SPEED_TARGET` mientras la línea sea visible. Antes: `speed_factor=0` cuando `line_error>0.45` (sensores de borde) → `desired_vel=0` → PI comandaba ángulo negativo de hasta -3° → retroceso. Ahora `desired_vel >= 0.25 * LINE_SPEED_TARGET` en todo momento con línea visible. | Fix de "saca tanto ángulo con sensores de borde que se va hacia atrás" |
| 2026-05-09 | Core/Src/main.c | I2C Bus Recovery en USER CODE BEGIN 2: antes de cualquier transacción I2C, de-init hi2c1, pines PB8/PB9 como GPIO OD, 9 pulsos SCL manuales + STOP condition, restaurar AF4, HAL_I2C_Init. Causa del freeze-en-boot: IWDG cortaba una transacción I2C → esclavo quedaba con SDA=LOW → siguiente boot se bloqueaba en HAL_I2C → loop IWDG→freeze→IWDG. Solo el corte de alimentación liberaba el bus. | Fix del freeze permanente post-IWDG-reset |
| 2026-05-09 | Core/Src/main.c | IWDG watchdog: timeout ~2s (LSI /256, reload=250). Init en USER CODE BEGIN 2, patada (`0xAAAA`) en cada iteración del while(1). Cubre cualquier freeze: I2C lockup, interrupt loop infinito, HardFault, etc. El IWDG no se puede detener una vez arrancado. | Auto-recuperación de freeze sin intervención manual |
| 2026-05-09 | Core/Src/stm32f4xx_it.c, main.c | Fix freeze/HardFault: (1) `HardFault_Handler` → `NVIC_SystemReset()` para auto-recuperación. (2) `EXTI15_10_IRQHandler`: guarda `if(EXTI->IMR & pin)` antes de llamar al handler de cada encoder — evita que el MPU (PB12) procese bits pendientes de encoders enmascarados. (3) `TIM5_IRQn` prioridad rebajada de 0→6 en USER CODE BEGIN 2 para que no preempte handlers de encoder (EXTI9_5 prio 5, EXTI15_10 prio 1). | HardFault/freeze al mover el robot físicamente |
| 2026-05-09 | Core/Src/main.c | Comportamiento sin línea: FOLLOWING→LOST threshold 1s→2s. LINE_STATE_LOST y LINE_STATE_SEARCHING unificados: `steering_adjustment=0`, setpoint con freno de posición (BALANCE_ONLY). Robot queda quieto hasta que vuelve a ver la línea. | Sin girar como loco cuando pierde la línea |
| 2026-05-09 | Core/Src/main.c | Detección de reversa en modo línea: si `velocity_est_f > 0.05` (reversa, vel positiva = atrás) acumula distancia; al superar 0.10 m (10 cm) sale a BALANCE_ONLY. Contador se resetea cuando avanza. | Límite de 10 cm en reversa durante seguidor de línea |
| 2026-05-09 | Core/Src/main.c | Regulación de velocidad en modo línea por encoder: `vel_scale = max(0, 1 - |velocity_est_f| / LINE_SPEED_TARGET)`. A mayor velocidad medida → menor ángulo de avance → robot frena naturalmente. Reemplaza el límite de pwm_sat por error (que causaba caídas) y el turn_scale por error de línea. |: `turn_pwm_limit = max(8, 40*(1-|line_error|*0.8))`. Recto: límite=40. Error=0.5: límite=24. Error=1 (borde): límite=8. Efecto inmediato sin esperar rampa del setpoint. | Aceleración excesiva en curvas del seguidor de línea |
| 2026-05-09 | Core/Src/main.c | Velocidad al girar: `base_setpoint_target = line_angle_cmd * turn_scale` donde `turn_scale = max(0.2, 1 - |line_error|*0.8)`. Centrado=100% velocidad, sensor borde=20%. Línea perdida (LINE_STATE_LOST/SEARCHING): `base_setpoint_target = trim`, `brake_setpoint_target = ComputeBrakeSetpointTarget(BALANCE_ONLY)` para quedarse quieto con control de posición. | Velocidad excesiva al girar + quedarse quieto cuando pierde línea |
| 2026-05-09 | Core/Src/main.c | Inner steering PI de encoders eliminado. `steering_adjustment = clamp(steering_cmd, ±20)` directo. El inner PI (diff_sp→diff_actual→LINE_STEER_FB_KP) anulaba la corrección si el signo de los encoders no era exacto. Ahora el giro es proporcional a `line_error` vía KP_LINE, ajustable en runtime desde Qt. | Fix de "no dobla cuando se va de la línea" |
| 2026-05-09 | Core/Src/main.c | Setpoint en modo línea simplificado: eliminadas doble rampa y suma de setpoint_trim. Ahora `base_setpoint_target = line_angle_cmd` directamente (0 si no ve línea). Tres clamps anteriores reducidos a uno en el PI de velocidad. Evita retroceso por trim negativo y simplifica el flujo. | Simplificación del setpoint en modo línea |
| 2026-05-09 | Core/Src/main.c | `MOTOR_LEFT_DEADBAND` agregado (igual estructura que `MOTOR_RIGHT_DEADBAND`). Motor izquierdo no se movía en balance con pwm_sat bajo porque no tenía compensación de zona muerta. Fix: aplica offset ±LEFT_DEADBAND al comando del motor izquierdo en el mismo punto que el derecho. Ajustar el valor si el izq. sigue sin arrancar. | Motor izquierdo inmóvil en balance con corrección pequeña |
| 2026-05-09 | Core/Src/main.c | `dynamic_setpoint_f` clampeado a ≥ 0 en modo línea (después del cálculo final). Bug: `setpoint_trim` negativo + `line_angle_ramped` pequeño → suma negativa → retroceso, aunque `line_angle_cmd` y `base_setpoint_target` estuviesen clampeados a ≥ 0. El `base_setpoint_f` (versión rampeada) podía quedar negativo durante la transición. | Fix definitivo de retroceso en modo línea |
| 2026-05-09 | Core/Src/main.c | `line_angle_cmd` y `base_setpoint_target` clampeados a `[0, LINE_ANGLE]` en modo línea (antes `[-LINE_ANGLE, LINE_ANGLE]`). El ángulo nunca va negativo → robot nunca retrocede durante el seguimiento. | Fix de retroceso esporádico en modo línea |
| 2026-05-09 | Core/Src/main.c | Lectura de sensores de línea: reemplazado `adcValues[]` (snapshot DMA crudo) por `adcAvg[]` (promedio móvil de 15 muestras a 4kHz). Eliminado filtro EMA redundante (ADC_BETA=0.8) que retrasaba la detección un ciclo (80% del valor en primer sample → no alcanzaba LINE_THRESHOLD). `LINE_THRESHOLD` bajado de 3000→1500 para calibrar contra adcAvg en lugar del valor crudo. | Fix de "pasa por encima de la línea sin corregir" |
| 2026-05-09 | Core/Src/main.c | `LINE_ANGLE` bajado de 3.0° → 1.5°. Con 3.0° el robot iba a fondo porque el PI de vel está en retroalimentación positiva y siempre satura al máximo. 1.5° mantiene suficiente avance para superar el steering pero limita la velocidad tope. Ajustable desde Qt en runtime. | Reducción de velocidad del seguidor de línea |
| 2026-05-15 | Core/Src/main.c, CLAUDE.md | PI de velocidad del seguidor de línea ahora regula velocidad de avance positiva (`forward_vel = max(0, -velocity_est_f)`) y compara contra `desired_forward_vel`. No cambia `turn_scale` ni mínimos de avance. | Evitar que una velocidad positiva de reversa reduzca el setpoint a cero y haga oscilar/retroceder |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Revertida la protección anti-retroceso que cortaba `pwm_sat` negativo a 0 y bloqueaba ruedas internas negativas con línea visible. | Ese enfoque hacía que al ver la línea el robot se fuera directamente hacia adelante y no mantuviera equilibrio |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Suavizado del avance en modo línea: `LINE_VEL_KP` 5.5→2.5, `LINE_VEL_KI` 0.8→0.25, `LINE_VEL_I_MAX` 3.0→1.0, rampa de setpoint de línea 1.0°→0.06° por ciclo y factor `stability_scale` según inclinación/gyro antes de pedir avance. | Reducir oscilación adelante/atrás cuando detecta la línea sin tocar el PWM final del balance |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Ajuste para recuperar avance sin volver a oscilar: `vel_scale` tiene piso 0.35, `stability_scale` tiene piso 0.50 y cae más lento, rampa de setpoint de línea 0.06°→0.12° por ciclo. | El cambio anterior estabilizó pero quedó demasiado conservador y no ganaba inclinación hacia adelante |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Agregado `line_forward_boost`: boost de inclinación basado en encoders. Si `line_forward_vel` queda por debajo de `LINE_FWD_STALL_FRAC` de la velocidad deseada, suma gradualmente hasta `LINE_FWD_BOOST_MAX=0.8°`; si recupera avance, lo descarga. | Usar encoders para pedir más ángulo cuando no avanza, sin tocar PWM final ni pelearse con el PID de balance |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Agregado `line_reverse_boost`: si `velocity_est_f` indica reversa (`> LINE_REV_VEL_START`), suma gradualmente hasta `LINE_REV_BOOST_MAX=1.4°` y evita que `stability_scale` baje de 0.75 mientras corrige reversa. | Controlar explícitamente cuando el robot se va hacia atrás, usando encoders y modificando solo el setpoint de inclinación |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Tres fixes para "se queda parado" en modo línea: (1) `vel_scale` eliminado (→1.0 constante) — el PI de velocidad ya regula la velocidad, vel_scale era un segundo freno redundante que recortaba el ángulo al 65%. (2) `sp_step_max` en LINE 0.12→0.25°/ciclo — rampa 2× más rápida. (3) Anti-windup al PI de velocidad: no acumula integral cuando la salida está saturada y `vel_error>0`, evita que el integral wind-up retrasa la frenada al superar el target. | Robot se quedaba parado en algunos puntos por rampa lenta y vel_scale recortando ángulo en el peor momento |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Fix doble que impedía ganar velocidad en modo línea: (1) `stability_scale` ahora usa `base_setpoint_f` como referencia en lugar de `SETPOINT_ANGLE+trim` (0°) — antes penalizaba la inclinación correcta de avance; umbral subido 0.8°→1.5°, piso 0.50→0.60, gyro 12→20 dps. (2) `vel_scale` floor 0.35→0.65 para que no recorte el ángulo en crucero. (3) `LINE_VEL_KI` 0.25→1.2 y `LINE_VEL_I_MAX` 1.0→2.5 para mantener ángulo en estado estacionario. | Con referencia errónea (0°), stability_scale reducía el ángulo a 52% cuando el robot inclinaba correctamente a 2°; combinado con vel_scale=0.35, el ángulo llegaba a 18% del comando |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | `line_forward_boost` (rampas stall/recover) reemplazado por corrección P directa basada en encoders: `line_enc_angle_corr = LINE_ENC_CORR_KP(2.5) × (deficit_vel / desired_vel)`, máx `LINE_ENC_CORR_MAX=2.0°`. Respuesta instantánea al déficit de velocidad vs. rampas lentas (35ms/° anterior). | Robot no ganaba ángulo al ver la línea desde parado: la corrección es proporcional y directa al dato de encoders |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Balance hold deshabilitado cuando `line_detected` en modo LINE_FOLLOWING: `if (robot_state == LINE_FOLLOWING && line_detected) balance_hold_active = 0`. También se limpia `balance_hold_active=0` en la transición de entrada al modo línea. | El hold silenciaba el PID durante ~80ms al arrancar el seguimiento (error < 0.5° mientras el setpoint rampea desde 0°), impidiendo que el robot ganara ángulo para avanzar sobre la línea |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Frenado activo por encoders: `line_angle_cmd` clampea hasta -1.5° (antes 0). Freno separado de `vel_scale` (cuando `line_angle_cmd < 0` no se multiplica por vel_scale=0 que lo anulaba). Integral del PI de velocidad solo acumula positivo; en sobrevelocidad decae ×0.95 sin acumular negativo → elimina retroceso por windup. `vel_scale` corta avance al 90% del target. `ADC_AVERAGE_SIZE` 15→4: retraso detección 7.5ms→0.5ms. Freno máximo -1.5° (suavizado desde -3°) para evitar oscilación acelera/frena. | Robot aceleraba sin límite, pasaba por encima de la línea sin detectarla, y oscilaba indefinidamente |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Detección de objetos + evasión por encoders: objeto detectado → `LINE_STATE_OBJ_ROTATE`. Giro derecha (`steering_adjustment=-20`) midiendo con encoders. `target_counts = (π/4 × OBJ_ROTATE_TRACK_WIDTH × ENC_CPR) / ENC_VEL_SCALE`. Métrica: `(|Δright| + |Δleft|) / 2`. Timeout 3s. `OBJ_ROTATE_TRACK_WIDTH=0.220m` (medido). Display muestra "ROTATE". | Reemplaza condición sensor-based (poco confiable) por medición odométrica de encoders |
| 2026-05-19 | Core/Src/main.c, CLAUDE.md | Fix de rotación en `LINE_STATE_OBJ_ROTATE`: reemplazado `steering_adjustment=-20` (que producía MotorControl(+10,+10) con pwm_sat≈0, sin diferencial real) por asignación directa de motores con `line_pivot_active=1`. Formula: `motorRightVelocity = -(pwm_sat + PIVOT_POWER)`, `motorLeftVelocity = -(pwm_sat - PIVOT_POWER)` con `PIVOT_POWER=30`. Esto produce MotorControl(-(pwm_sat+30), -(pwm_sat-30)) — ambos canales en la misma dirección relativa, creando spin real independiente del valor de pwm_sat. | Bug: la lógica half_steer falla a pwm_sat≈0 porque ambos motores terminan con el mismo signo positivo y el PID de balance lo cancela; el robot no rotaba y el timeout de 3s disparaba siempre |
| 2026-05-19 | Core/Src/main.c, CLAUDE.md | Fix de entrada inmediata a HOLD: variables `rot_initialized`, `rot_r0`, `rot_l0`, `rot_start_ms` del case OBJ_ROTATE eran `static` locales. Al caer el robot durante la rotación, el bloque LINE_FOLLOWING se salteaba (f_fallen=1) sin resetear esas vars. Al recuperarse, `dr/dl` acumulaban los counts de la caída → `rot_counts >= target_counts` inmediatamente → salto a OBJ_HOLD. Fix: variables promovidas a scope de archivo (`obj_rot_*`) y `obj_rot_initialized=0` se resetea en la transición FOLLOWING→OBJ_ROTATE. | Las statics locales no se pueden resetear desde fuera del case; se necesita scope de archivo para controlarlo explícitamente |
| 2026-05-19 | Core/Src/main.c, CLAUDE.md | Agregado `LINE_STATE_OBJ_REVERSE`: reversa de 300 counts con steering invertido basado en `line_error` (×0.3, clamped ±12) para mantener centrado sobre la línea mientras retrocede. Setpoint = trim − 1.5° para lean suave hacia atrás. Sin línea detectada: retrocede recto. Corregido `target_counts` de OBJ_ROTATE: fórmula geométrica daba 23 pero en la práctica son 350 → valor empírico. Cadena: FOLLOWING → OBJ_REVERSE → OBJ_BRAKE → OBJ_ROTATE → OBJ_HOLD. | Fórmula ignoraba reducción de engranajes reales; steering en reversa usa signo negado del error de línea. |
| 2026-05-19 | Core/Src/main.c, CLAUDE.md | Agregado `LINE_STATE_OBJ_BRAKE`: estado intermedio post-reversa que usa `ComputeBrakeSetpointTarget` para frenar activamente por encoders y espera a que `fabsf(velocity_est_f) < 0.05 m/s` antes de iniciar el giro. Timeout de 1500 ms. Evita que la inercia de la reversa distorsione el giro. | Sin este estado, el robot entraba al giro con inercia residual y no giraba en el lugar. |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | `LINE_STATE_OBJ_HOLD` simplificado: usa exactamente las mismas características que BALANCE_ONLY (setpoint=trim, freno traslacional por encoders, corrección yaw=gz×0.3 igual que LOST/SEARCHING). Sale únicamente cuando `f_fallen=1` → transiciona a `LINE_STATE_FOLLOWING` para retomar la línea al recuperarse. Display muestra "HOLD". | Reutiliza el balance estático probado en lugar de lógica custom; sale solo por caída |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Ventana de ignorar objeto al activar modo línea por botón (doble click): `obj_detect_ignore_until_ms = HAL_GetTick() + 4000`. La detección de objeto no dispara mientras `HAL_GetTick() < obj_detect_ignore_until_ms`. Solo aplica al activar por botón físico, no por comando Qt. | Evitar falsa detección cuando el dedo pasa frente a los ADC 5-8 al presionar el botón |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Pantalla 1 (LINE_FOLLOWING): panel izquierdo expandido de 4 a 8 barras ADC (bar_width 5→7, spacing 1, área 55→70px, divisor 57→71, contenido derecho 62→73). ADC 1-4 (línea) en orden invertido; ADC 5-8 (objeto) a continuación. Separador punteado en x=32. | Barras más anchas y legibles; panel derecho desplazado para dar espacio |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Pantalla 4 (f_change_display==4): reemplazado bloque ADC1-4 con Font_7x10 por grid 4×2 con Font_5x7 mostrando ADC1-8. Columna izquierda (x=1): A1-A4; columna derecha (x=67): A5-A8. Divisor vertical en x=63. | Ver todos los ADC raw en una sola pantalla para calibrar umbrales de línea y objeto |
| 2026-05-18 | Core/Src/main.c, CLAUDE.md | Watchdog de software I2C/MPU: función `I2C1_Recover()` (DMA abort + 9 pulsos SCL + HAL_I2C_Init + I2C_Manager_Init) llamada desde while(1) si pasan >150ms sin dato nuevo del MPU. Watchdog integrado en el bloque `mpu_data_ready_for_ctrl` usando `last_ctrl_ms` estático. Imprime "I2C RECOVER" por USB para diagnóstico. | Fix del freeze total donde el while(1) seguía girando pero el DMA I2C quedaba colgado sin completar, dejando ControlStep10ms sin correr y todo congelado |

---

## Decisiones de Diseño
> Registrar el *por qué* de decisiones críticas del firmware.

- **Filtro complementario en lugar de Kalman:** menor costo computacional, suficiente para este sistema (α=0.98, dt=10ms)
- **Loop de control en callback de TIM1 a 100 Hz:** periodicidad exacta garantizada por hardware, independiente del loop main
- **Bias MPU hardcodeado (`MPU_USE_FIXED_BIAS`):** elimina calibración al arranque; el robot puede actuar en segundos sin necesidad de estar quieto
- **I2C no bloqueante con cola:** el MPU y el SSD1306 comparten I2C1; la cola evita colisiones y no bloquea el loop de 10ms
- **Protocolo UNER binario:** frame compacto con checksum, permite comandos bidireccionales y telemetría eficiente sobre USB CDC y UDP
- **USB CDC en lugar de UART para Qt:** mayor throughput y sin necesidad de conversor USB-UART externo
- **USART1 dedicado a ESP-01:** recepción byte-a-byte por interrupción, sin DMA para UART (el ESP-01 maneja su propia lógica AT)
- **PWM a ~100 kHz (TIM3/TIM4, Period=959):** frecuencia alta para reducir ruido audible y mejorar respuesta de motores DC
- **Masking de EXTI en encoder:** tras cada pulso se enmascara la línea EXTI y TIM5 la reactiva cada 2ms. Limita a 500 Hz/canal, evita freeze por rafagas. Solución más robusta que solo limpiar flags
- **4x quadrature por software:** ambos canales A y B decodificados por transición de estados. ENC_CPR=28. Sin modo encoder de hardware (requeriría cambio de pines)
- **Encoders por polling (TIM2 @ 4kHz) en lugar de EXTI (2026-07-01):** el esquema anterior por interrupción de flanco con masking anti-storm (EXTI + TIM5 re-habilitando cada 2ms) compartía prioridad NVIC con el DMA de I2C y quedó sospechado de causar freezes/resets recurrentes durante el giro de 180°, posiblemente por ráfagas de interrupciones o una carrera de lectura-modificación-escritura sobre `EXTI->IMR` entre distintos niveles de prioridad. El muestreo periódico tiene una tasa de interrupción fija y acotada (4000/seg) independiente de la velocidad física de la rueda, eliminando esa clase de problema de raíz, a costa de una resolución temporal ligeramente menor (peor caso 250µs de retraso en detectar un flanco, insignificante frente a los 10ms del loop de control)
- **`KV_brake_value` mapeado a slider "KV" en Qt:** permite ajustar el freno fuerte en runtime sin recompilar. Se inicializa desde `KV_BRAKE_STRONG`
- **Seguidor de línea con setpoint no negativo:** cuando la línea está visible, el setpoint de inclinación no debe ir a retroceso. No forzar `pwm_sat` a 0 ni bloquear ruedas internas negativas: esa prueba del 2026-05-17 hizo que el robot empujara hacia adelante sin poder equilibrarse y fue revertida el 2026-05-18.
- **Avance de línea subordinado a estabilidad:** el seguidor de línea solo modula el setpoint de inclinación. Si `filtered_roll_deg` se aleja del equilibrio o `gyro_f` sube, `stability_scale` reduce el avance pedido. El PWM final sigue bajo autoridad del PID de balance.
- **Boost de avance por encoders:** `line_forward_boost` aumenta solo el setpoint de inclinación cuando los encoders muestran que el robot no avanza lo suficiente en modo línea. No actúa sobre motores ni `pwm_sat`; el PID de balance conserva la autoridad final.
- **Escape de reversa por encoders:** si `velocity_est_f` es positiva (convención actual: reversa), `line_reverse_boost` suma inclinación hacia adelante de forma rampeada. Es más fuerte que el boost de stall, pero sigue actuando solo sobre setpoint.
- **Steering de línea directo:** el PI interno sobre diferencial de encoders quedó deshabilitado/eliminado del camino activo porque dependía de signos de encoder muy sensibles. El giro actual usa `steering_adjustment = clamp(KP_LINE*line_error + KI_LINE*I + KD_LINE*D, ±20)` y los encoders quedan para velocidad longitudinal.

---

## Dependencias con Qt
> Cambios en el firmware que requieren cambios **coordinados** en Qt:

- Si modificás el **formato de trama UNER** → actualizar el parser en `serialmanager.cpp` de Qt
- Si cambiás **campos de `WifiLogData_t`** → actualizar el display/plot en Qt (struct packed compartida)
- Si cambiás **puertos UDP** (30000/30010) → actualizar `udpmanager.cpp` en Qt
- Si agregás **nuevos comandos** al enum `_eCmd` → agregar handlers en Qt
- **Odometría (2026-07-04)**: para usar `GET_ODOMETRY=0xDA` (respuesta: 3 floats LE = x[m], y[m], θ[°]) y `RESET_ODOMETRY=0xDB` (ACK) hay que agregar sus handlers en Qt (`serialmanager.cpp` / `udpmanager.cpp`). Son aditivos: mientras Qt no los mande, nada cambia
- Consultar siempre: `C:\Microcontroladores\BalancinQT\CLAUDE.md`

---

## Comandos / Flujo de Trabajo en STM32CubeIDE
```
1. Abrir workspace: C:\Users\tadeo\STM32CubeIDE\workspace_1.18.1\
2. Proyecto: Balancin_Mendelevich
3. Para regenerar código HAL: abrir Balancin_Mendelevich.ioc → Generate Code
   ⚠️  NO sobreescribir secciones USER CODE BEGIN / USER CODE END
4. Compilar: Project → Build All (Ctrl+B)
5. Flashear: Run → Debug (F11) o Run (Ctrl+F11)
6. Monitor serie: Window → Show View → Console  (o usar Qt para debug)
```

## ⚠️ Advertencias Importantes
- **Nunca** modificar código fuera de bloques `/* USER CODE BEGIN */` y `/* USER CODE END */` — CubeMX los sobreescribirá.
- **Después de cada regeneración de CubeMX:** verificar con `git diff` que no se hayan borrado handlers críticos en `stm32f4xx_it.c`. CubeMX elimina `EXTI9_5_IRQHandler` (encoder PA8) y simplifica `EXTI15_10_IRQHandler` quitando el manejo de encoders PB13/14/15 — sin esos handlers el CPU cae en loop infinito al primer pulso de encoder. También verificar que las prioridades de interrupts en `MX_DMA_Init` y `MX_GPIO_Init` no hayan sido reseteadas a 0. CubeMX resetea silenciosamente `DMA1_Stream0/1_IRQn`, `DMA2_Stream0_IRQn` y `EXTI15_10_IRQn` a prio 0, lo que causa freeze I2C. Las prioridades correctas están forzadas en `USER CODE BEGIN 2` y sobreviven la regeneración — pero siempre confirmar con git diff.
- Antes de cambiar el `.ioc`, hacer commit en Git o guardar backup.
- El módulo WiFi puede tardar hasta 3s en conectarse al arranque — normal.
- El SSID/password/IP WiFi está hardcodeado en `main.c` líneas ~242-244 y **cambia según la red donde se trabaje** — ver tabla de perfiles en la sección "Canal 2 — WiFi UDP". Siempre verificar antes de flashear.
